// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Database` (manifest: lucide: "database").
// Drop icons-src/outlined/database.svg in place and re-run `pnpm icons:build`.
import { Database as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** DatabaseIcon — stacked cylinder */
export function DatabaseIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="database"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
DatabaseIcon.displayName = "DatabaseIcon"
