// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `FolderOpen` (manifest: lucide: "folder-open").
// Drop icons-src/outlined/folder-open.svg in place and re-run `pnpm icons:build`.
import { FolderOpen as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** FolderOpenIcon — open folder */
export function FolderOpenIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="folder-open"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
FolderOpenIcon.displayName = "FolderOpenIcon"
