// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `ChevronsDownUp` (manifest: lucide: "chevrons-down-up").
// Drop icons-src/outlined/caret-collapse.svg in place and re-run `pnpm icons:build`.
import { ChevronsDownUp as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** CaretCollapseIcon — two chevrons pointing toward each other vertically (down over up) */
export function CaretCollapseIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="caret-collapse"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
CaretCollapseIcon.displayName = "CaretCollapseIcon"
