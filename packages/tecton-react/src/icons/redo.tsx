// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Redo` (manifest: lucide: "redo").
// Drop icons-src/outlined/redo.svg in place and re-run `pnpm icons:build`.
import { Redo as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** RedoIcon — curved arrow to the right */
export function RedoIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="redo"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
RedoIcon.displayName = "RedoIcon"
