// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `SquareKanban` (manifest: lucide: "square-kanban").
// Drop icons-src/outlined/kanban.svg in place and re-run `pnpm icons:build`.
import { SquareKanban as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** KanbanIcon — square with three vertical bars of different height */
export function KanbanIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="kanban"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
KanbanIcon.displayName = "KanbanIcon"
