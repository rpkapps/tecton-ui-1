// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Upload` (manifest: lucide: "upload").
// Drop icons-src/outlined/export-upload.svg in place and re-run `pnpm icons:build`.
import { Upload as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** ExportUploadIcon — arrow up out of a tray */
export function ExportUploadIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="export-upload"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
ExportUploadIcon.displayName = "ExportUploadIcon"
