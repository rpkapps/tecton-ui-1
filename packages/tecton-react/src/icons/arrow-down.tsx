// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `ArrowDown` (manifest: lucide: "arrow-down").
// Drop icons-src/outlined/arrow-down.svg in place and re-run `pnpm icons:build`.
import { ArrowDown as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** ArrowDownIcon — arrow pointing down */
export function ArrowDownIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="arrow-down"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
ArrowDownIcon.displayName = "ArrowDownIcon"
