// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Mic` (manifest: lucide: "mic").
// Drop icons-src/outlined/microphone.svg in place and re-run `pnpm icons:build`.
import { Mic as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** MicrophoneIcon — microphone */
export function MicrophoneIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="microphone"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
MicrophoneIcon.displayName = "MicrophoneIcon"
