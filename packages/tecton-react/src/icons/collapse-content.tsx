// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Minimize` (manifest: lucide: "minimize").
// Drop icons-src/outlined/collapse-content.svg in place and re-run `pnpm icons:build`.
import { Minimize as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** CollapseContentIcon — two corner brackets with short arrows pointing inward */
export function CollapseContentIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="collapse-content"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
CollapseContentIcon.displayName = "CollapseContentIcon"
