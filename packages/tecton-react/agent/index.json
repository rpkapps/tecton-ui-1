{
 "$comment": "<!-- Generated by `pnpm --filter @tecton/react agent:build` from guidelines/ — do not edit. -->",
 "package": "@tecton/react",
 "version": "0.1.0",
 "synonyms": {
  "modal": [
   "dialog"
  ],
  "popup": [
   "popover",
   "dialog"
  ],
  "lightbox": [
   "dialog"
  ],
  "flyout": [
   "sheet"
  ],
  "slideover": [
   "sheet"
  ],
  "drawer": [
   "drawer",
   "sheet"
  ],
  "dropdown": [
   "select",
   "menu"
  ],
  "picker": [
   "select"
  ],
  "typeahead": [
   "combobox"
  ],
  "autocomplete": [
   "combobox"
  ],
  "autosuggest": [
   "combobox"
  ],
  "searchable": [
   "combobox"
  ],
  "snackbar": [
   "toast"
  ],
  "notification": [
   "toast"
  ],
  "notify": [
   "toast"
  ],
  "banner": [
   "alert"
  ],
  "callout": [
   "alert"
  ],
  "loader": [
   "spinner",
   "skeleton"
  ],
  "loading": [
   "spinner",
   "skeleton"
  ],
  "pill": [
   "badge",
   "chip"
  ],
  "tag": [
   "chip"
  ],
  "kpi": [
   "stat"
  ],
  "metric": [
   "stat"
  ],
  "readout": [
   "stat"
  ],
  "grid": [
   "table"
  ],
  "datagrid": [
   "table"
  ],
  "spreadsheet": [
   "table"
  ],
  "rows": [
   "table"
  ],
  "hierarchy": [
   "tree"
  ],
  "sidenav": [
   "sidebar"
  ],
  "nav": [
   "sidebar",
   "navigation"
  ],
  "toggle": [
   "switch",
   "toggle"
  ],
  "textbox": [
   "input"
  ],
  "textfield": [
   "input"
  ],
  "multiline": [
   "textarea"
  ],
  "hotkey": [
   "shortcut",
   "kbd"
  ],
  "keybinding": [
   "shortcut",
   "kbd"
  ],
  "keyboard": [
   "kbd",
   "shortcut"
  ],
  "profile": [
   "avatar"
  ],
  "slideshow": [
   "carousel"
  ],
  "expand": [
   "accordion",
   "collapsible"
  ],
  "collapse": [
   "collapsible",
   "accordion"
  ],
  "pane": [
   "resizable",
   "panel"
  ],
  "datepicker": [
   "calendar",
   "date"
  ],
  "zero": [
   "empty"
  ],
  "nothing": [
   "empty"
  ],
  "confirm": [
   "confirmation"
  ],
  "irreversible": [
   "destructive"
  ],
  "delete": [
   "destructive"
  ],
  "hint": [
   "tooltip"
  ],
  "preview": [
   "hover"
  ],
  "chat": [
   "message",
   "conversation"
  ],
  "assistant": [
   "message",
   "conversation"
  ],
  "upload": [
   "attachment"
  ],
  "file": [
   "attachment"
  ],
  "divider": [
   "separator"
  ],
  "placeholder": [
   "skeleton"
  ],
  "gauge": [
   "meter"
  ],
  "capacity": [
   "meter"
  ],
  "donut": [
   "circular"
  ],
  "cmdk": [
   "command"
  ],
  "rightclick": [
   "context"
  ],
  "toolbar": [
   "toolbar",
   "group"
  ],
  "bulk": [
   "action"
  ],
  "clipboard": [
   "copy"
  ],
  "paging": [
   "pagination"
  ],
  "launcher": [
   "finder"
  ],
  "switcher": [
   "finder"
  ],
  "truncate": [
   "overflow"
  ],
  "ellipsis": [
   "overflow"
  ],
  "rtl": [
   "direction"
  ],
  "form": [
   "field"
  ],
  "validation": [
   "field",
   "error"
  ],
  "dark": [
   "theme"
  ],
  "light": [
   "theme"
  ],
  "colour": [
   "color"
  ],
  "pin code": [
   "otp"
  ],
  "verification code": [
   "otp"
  ],
  "progress ring": [
   "circular"
  ],
  "split view": [
   "resizable"
  ],
  "split pane": [
   "resizable"
  ],
  "horizontal rule": [
   "separator"
  ],
  "page numbers": [
   "pagination"
  ],
  "next page": [
   "pagination"
  ],
  "percent": [
   "progress"
  ],
  "percentage": [
   "progress"
  ],
  "per page": [
   "pagination"
  ],
  "design token": [
   "theme"
  ]
 },
 "entries": [
  {
   "id": "accordion",
   "kind": "component",
   "name": "Accordion",
   "import": "import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from \"@tecton/react/components/accordion\"",
   "family": "layout",
   "summary": "Several independent sections stack vertically and the reader opens the ones they need: an FAQ, grouped settings, a long form in parts.",
   "fields": {
    "names": "accordion accordion accordion item accordion trigger accordion content",
    "useWhen": "Several independent sections stack vertically and the reader opens the ones they need: an FAQ, grouped settings, a long form in parts. Every section stays on the page; opening one does not replace another. The set is small and known, so rendering every panel up front costs nothing.",
    "routed": "several stacked sections with one open at a time stacked sections that expand, with no parent and child structure",
    "do": "Give every `AccordionItem` a `value`: that string is what `defaultValue`, `value` and `onValueChange` hold — always an **array**, even when one section opens at a time. Allow several sections at once with `multiple` (the default is one), and disable one with `disabled` on its `AccordionItem`. Put the heading text straight into `AccordionTrigger`: it renders the heading, the button and the chevron pair itself, so `className` on `Accordion` is for width and frame only (`max-w-lg`, `border`)."
   },
   "notFor": [
    {
     "need": "one region that opens and closes on its own",
     "id": "collapsible"
    },
    {
     "need": "sections that replace each other in the same space",
     "id": "tabs"
    },
    {
     "need": "an expandable hierarchy of folders and records",
     "id": "tree-view"
    }
   ],
   "related": [
    "collapsible",
    "tabs",
    "tree-view"
   ],
   "exports": [
    "Accordion",
    "AccordionItem",
    "AccordionTrigger",
    "AccordionContent"
   ],
   "aliases": [],
   "tokens": 950
  },
  {
   "id": "action-bar",
   "kind": "component",
   "name": "ActionBar",
   "import": "import { ActionBar, ActionBarSelection, ActionBarMessage, ActionBarActions, actionBarVariants } from \"@tecton/react/tecton/action-bar\"",
   "family": "actions",
   "summary": "Rows are selected in a table or list and the actions apply to that selection.",
   "fields": {
    "names": "action bar action bar action bar selection action bar message action bar actions action bar variants",
    "useWhen": "Rows are selected in a table or list and the actions apply to that selection. A form has unsaved changes and needs Save or Discard without moving the page. The bar should exist only while there is something to act on, then disappear.",
    "routed": "a bar of actions for the current selection actions that appear only while rows are selected",
    "do": "Control it with `open` and dismiss with `onDismiss`; the bar owns Escape and the enter transition. Give it an `aria-label`; it renders a `div` with `role=\"region\"`. Summarise with `ActionBarSelection` (`count`, `total`, `label`, `onClear`) or with `ActionBarMessage`. Put the actions in `ActionBarActions`, wrapping collapsible ones in `OverflowItem` and leaving the primary action bare. Choose `placement=\"toolbar\"` to fill a table's toolbar row, `\"floating\"` for a sticky card inside the scroll container."
   },
   "notFor": [
    {
     "need": "a permanent row of page actions",
     "id": "button-group"
    },
    {
     "need": "a transient confirmation that carries no actions",
     "id": "sonner"
    },
    {
     "need": "a list of actions behind a single button",
     "id": "dropdown-menu"
    }
   ],
   "related": [
    "button-group",
    "dropdown-menu"
   ],
   "exports": [
    "ActionBar",
    "ActionBarSelection",
    "ActionBarMessage",
    "ActionBarActions",
    "actionBarVariants"
   ],
   "aliases": [],
   "tokens": 979
  },
  {
   "id": "alert",
   "kind": "component",
   "name": "Alert",
   "import": "import { Alert, AlertTitle, AlertDescription, AlertAction, alertVariants } from \"@tecton/react/components/alert\"",
   "family": "feedback",
   "summary": "A message must stay on the page until the user reads or resolves it: a failed run, unsaved changes, a stale model.",
   "fields": {
    "names": "alert alert alert title alert description alert action alert variants",
    "useWhen": "A message must stay on the page until the user reads or resolves it: a failed run, unsaved changes, a stale model. The message belongs to a region — above a form, inside a panel, at the top of a page — not to a single field. The message needs a severity colour, and sometimes an action beside it.",
    "routed": "a message about something that failed on a page that does have content a message that must stay until the user resolves it",
    "do": "`variant` is the severity (`default`, `info`, `success`, `warning`, `destructive`); `appearance` is the surface (`default` keeps the card background, `outline` is a coloured border, `filled` is the Tecton status surface). Compose `Alert > icon, AlertTitle, AlertDescription, AlertAction`, with the icon as a direct child of `Alert`. Put every button, including the dismiss control, inside `AlertAction` and wire it with `onClick`; a decision that blocks the user is an `AlertDialog`, not an alert. `className` is for width and placement (`max-w-md`, `mb-4`), never for the status colours."
   },
   "notFor": [
    {
     "need": "a confirmation that should disappear on its own",
     "id": "sonner"
    },
    {
     "need": "the state of a list or a page that has nothing in it",
     "id": "empty"
    },
    {
     "need": "a one-word status label inside a row or a header",
     "id": "badge"
    },
    {
     "need": "a message about a single form field",
     "id": "field"
    }
   ],
   "related": [
    "sonner",
    "empty"
   ],
   "exports": [
    "Alert",
    "AlertTitle",
    "AlertDescription",
    "AlertAction",
    "alertVariants"
   ],
   "aliases": [],
   "tokens": 1192
  },
  {
   "id": "alert-dialog",
   "kind": "component",
   "name": "AlertDialog",
   "import": "import { AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogMedia, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogCancel, AlertDialogAction, AlertDialogOverlay, AlertDialogPortal } from \"@tecton/react/components/alert-dialog\"",
   "family": "overlays",
   "summary": "An action destroys or overwrites something: delete, discard, overwrite, revoke.",
   "fields": {
    "names": "alert dialog alert dialog alert dialog trigger alert dialog content alert dialog header alert dialog media alert dialog title alert dialog description alert dialog footer alert dialog cancel alert dialog action alert dialog overlay alert dialog portal",
    "useWhen": "An action destroys or overwrites something: delete, discard, overwrite, revoke. The consequence cannot be undone once the user continues. The user must answer: there is no outside-press dismissal and no close button.",
    "routed": "a confirmation before a destructive action a confirmation of a destructive action a confirmation the user has to answer",
    "do": "Compose `AlertDialog` > `AlertDialogTrigger render={<Button variant=\"destructive\" />}` + `AlertDialogContent`, as for `Dialog`. End the title with a question and state the consequence in `AlertDialogDescription`. `AlertDialogCancel` closes the prompt; `AlertDialogAction` is a plain `Button`, so control the dialog with `open` / `onOpenChange` and close it in the action's `onClick` once the work is done. Colour the confirm with `variant=\"destructive\"` on `AlertDialogAction` — never with `className` — and use `size=\"sm\"` on `AlertDialogContent` with `AlertDialogMedia` for a short, centred prompt."
   },
   "notFor": [
    {
     "need": "a form, or a decision the user may walk away from",
     "id": "dialog"
    },
    {
     "need": "a confirmation of something the user can undo afterwards",
     "id": "sonner"
    }
   ],
   "related": [
    "dialog"
   ],
   "exports": [
    "AlertDialog",
    "AlertDialogTrigger",
    "AlertDialogContent",
    "AlertDialogHeader",
    "AlertDialogMedia",
    "AlertDialogTitle",
    "AlertDialogDescription",
    "AlertDialogFooter",
    "AlertDialogCancel",
    "AlertDialogAction",
    "AlertDialogOverlay",
    "AlertDialogPortal"
   ],
   "aliases": [],
   "tokens": 1101
  },
  {
   "id": "app-finder",
   "kind": "component",
   "name": "AppFinder",
   "import": "import { AppFinder, AppFinderTrigger, AppFinderMenu, AppFinderInput, AppFinderList, AppFinderGroup, AppFinderItem, AppFinderIcon, appFinderIconVariants } from \"@tecton/react/tecton/app-finder\"",
   "family": "navigation",
   "summary": "The shell header switches between independently deployed applications.",
   "fields": {
    "names": "app finder app finder app finder trigger app finder menu app finder input app finder list app finder group app finder item app finder icon app finder icon variants",
    "useWhen": "The shell header switches between independently deployed applications. The catalogue is large enough that a flat menu stops working: categories, search, short codes. The current application has to stay visible in the header as a tinted code tile.",
    "routed": "switching between applications from the header",
    "do": "Compose `AppFinder` > (`AppFinderTrigger`, `AppFinderMenu` > `AppFinderInput` + `AppFinderList` > `AppFinderGroup` > `AppFinderItem`). Describe the current app on the trigger: the short code as children, `name` for the label beside the tile, the category's `tone`. Navigate from `onSelect` on `AppFinderList`; it receives the item's `value` and closes the popover afterwards. Control the popover with `open` / `onOpenChange` on `AppFinder` when you need to. Give every item a unique `value` across groups, `keywords` for its short code and category, and `current` on the mounted one; the filter matches `name` and `keywords`, never `value`. Reuse `AppFinderIcon` wherever else the tile is needed — a launcher grid, a command palette row."
   },
   "notFor": [
    {
     "need": "a short list of actions opened from a header button",
     "id": "dropdown-menu"
    },
    {
     "need": "a palette of commands and records inside one application",
     "id": "command"
    },
    {
     "need": "the global action cluster at the end of the shell header",
     "id": "app-shell"
    }
   ],
   "related": [
    "command",
    "app-shell"
   ],
   "exports": [
    "AppFinder",
    "AppFinderTrigger",
    "AppFinderMenu",
    "AppFinderInput",
    "AppFinderList",
    "AppFinderGroup",
    "AppFinderItem",
    "AppFinderIcon",
    "appFinderIconVariants"
   ],
   "aliases": [],
   "tokens": 926
  },
  {
   "id": "app-shell",
   "kind": "component",
   "name": "AppShell",
   "import": "import { AppShell, AppShellHeader, AppShellBrand, AppShellNav, AppShellActions, AppShellAction, AppShellCommandTrigger, AppShellDivider, AppShellOverflow, AppShellUserMenu, AppShellBody, AppShellSidebar, AppShellMain, AppShellAside, AppShellSplit, AppShellSplitPanel, AppShellSplitHandle, useMinWidth } from \"@tecton/react/tecton/app-shell\"",
   "family": "surfaces",
   "summary": "The outermost frame of an application: a top bar, an optional left rail, a work area, an optional right aside.",
   "fields": {
    "names": "app shell app shell app shell header app shell brand app shell nav app shell actions app shell action app shell command trigger app shell divider app shell overflow app shell user menu app shell body app shell sidebar app shell main app shell aside app shell split app shell split panel app shell split handle use min width",
    "useWhen": "The outermost frame of an application: a top bar, an optional left rail, a work area, an optional right aside. The frame fills the viewport (`h-svh`) and clips its own overflow, so each region scrolls on its own. The header carries the host's own cluster — search, help, settings, the account menu — so every mounted application shows the same set. A tool panel lives beside the content and the user may drag the divider between them.",
    "routed": "the global action cluster at the end of the shell header the application frame around the work area the top bar of the application frame the application frame around the work area a draggable divider between the regions of the application frame the scrolling work area of the application frame the borders between the regions of the application frame a fixed navigation column inside the application frame the header, rail and work area of an application that owns the document",
    "do": "Compose it: `AppShellHeader` (with `AppShellBrand`, `AppShellNav`, `AppShellActions`), then `AppShellBody` holding `AppShellSidebar`, `AppShellMain` and `AppShellAside`. Put the page content in `AppShellMain`; it is the scrolling region of the work area. Give every `AppShellAction` a `label` — both the accessible name and the tooltip — separate groups with `AppShellDivider`, and on a narrow header hide the low-priority ones (`hidden lg:inline-flex`) in favour of `AppShellOverflow` (`lg:hidden`). For a draggable divider, wrap the regions in `AppShellSplit` with an `AppShellSplitPanel` each and an `AppShellSplitHandle` between them, and gate a full-height aside on `useMinWidth(1280)`. Inside an `AppShellSplitPanel` give the `AppShellAside` `className=\"h-full w-full border-s-0\"`; otherwise `className` overrides the height alone (`h-full` in an embedded context) and the shell keeps its surfaces and borders."
   },
   "notFor": [
    {
     "need": "a titled tool surface inside the work area",
     "id": "panel"
    },
    {
     "need": "the title block at the top of the page inside the main area",
     "id": "page-header"
    },
    {
     "need": "actions that belong to the page rather than to the shell",
     "id": "page-header"
    },
    {
     "need": "a panel that floats over the page and is dismissed",
     "id": "sheet"
    },
    {
     "need": "a toolbar that folds into a More menu by measured width",
     "id": "overflow"
    },
    {
     "need": "switching between applications from the header",
     "id": "app-finder"
    }
   ],
   "related": [
    "panel",
    "page-header",
    "sheet",
    "app-finder",
    "overflow"
   ],
   "exports": [
    "AppShell",
    "AppShellHeader",
    "AppShellBrand",
    "AppShellNav",
    "AppShellActions",
    "AppShellAction",
    "AppShellCommandTrigger",
    "AppShellDivider",
    "AppShellOverflow",
    "AppShellUserMenu",
    "AppShellBody",
    "AppShellSidebar",
    "AppShellMain",
    "AppShellAside",
    "AppShellSplit",
    "AppShellSplitPanel",
    "AppShellSplitHandle",
    "useMinWidth"
   ],
   "aliases": [],
   "tokens": 1261
  },
  {
   "id": "aspect-ratio",
   "kind": "component",
   "name": "AspectRatio",
   "import": "import { AspectRatio } from \"@tecton/react/components/aspect-ratio\"",
   "family": "layout",
   "summary": "Media has to hold a shape before it loads, so the surrounding layout never jumps: a thumbnail grid, a card cover, a video embed.",
   "fields": {
    "names": "aspect ratio aspect ratio",
    "useWhen": "Media has to hold a shape before it loads, so the surrounding layout never jumps: a thumbnail grid, a card cover, a video embed. The box should follow its available width rather than a height you picked. The ratio is the design decision — 16:9, 1:1, 9:16 — not an approximate height.",
    "routed": "",
    "do": "Pass the ratio as a number: `ratio={16 / 9}`, `ratio={1}`, `ratio={9 / 16}`. It is required and goes straight into `style={{ \"--ratio\": ratio }}`. Make the child fill the box, with `absolute inset-0 size-full object-cover` on an `img`, or `fill` on a framework `Image`; the component only sets `relative`. Control the width from outside (`className=\"w-full max-w-sm\"`) and let the height follow. Put the radius and the placeholder tone on the `AspectRatio` itself (`rounded-lg bg-muted`) so they show while the media is still loading."
   },
   "notFor": [
    {
     "need": "a placeholder shape while the content loads",
     "id": "skeleton"
    },
    {
     "need": "a round picture of a person or an account",
     "id": "avatar"
    },
    {
     "need": "a full-bleed map, schematic or 3D view",
     "id": "canvas"
    }
   ],
   "related": [
    "avatar",
    "skeleton",
    "carousel"
   ],
   "exports": [
    "AspectRatio"
   ],
   "aliases": [],
   "tokens": 901
  },
  {
   "id": "attachment",
   "kind": "component",
   "name": "Attachment",
   "import": "import { Attachment, AttachmentGroup, AttachmentMedia, AttachmentContent, AttachmentTitle, AttachmentDescription, AttachmentActions, AttachmentAction, AttachmentTrigger } from \"@tecton/react/components/attachment\"",
   "family": "conversation",
   "summary": "A file or an image the user attached: in the composer before sending, or inside a message.",
   "fields": {
    "names": "attachment attachment attachment group attachment media attachment content attachment title attachment description attachment actions attachment action attachment trigger",
    "useWhen": "A file or an image the user attached: in the composer before sending, or inside a message. The upload lifecycle has to be visible: selected, uploading, processing, failed, done. The card opens something — a preview dialog, a download — while remove or retry stays separate.",
    "routed": "a file or an image card inside a message or a composer",
    "do": "Compose it: `AttachmentMedia`, then `AttachmentContent` with `AttachmentTitle` and `AttachmentDescription`, then `AttachmentActions`. Drive the look with `state=\"idle\" | \"uploading\" | \"processing\" | \"error\" | \"done\"`, `size` and `orientation`. Use `AttachmentMedia variant=\"image\"` around an `<img>` and `orientation=\"vertical\"` for a thumbnail; lay several cards out in an `AttachmentGroup`. Give every `AttachmentAction` an `aria-label` naming the action and the file; it is a `Button`, so press it with `onClick`."
   },
   "notFor": [
    {
     "need": "a short static label such as a file type or a count",
     "id": "badge"
    },
    {
     "need": "a tag the user can select or remove from a filter row",
     "id": "chip"
    },
    {
     "need": "a row of a file browser or a settings list",
     "id": "item"
    }
   ],
   "related": [
    "message",
    "badge",
    "item"
   ],
   "exports": [
    "Attachment",
    "AttachmentGroup",
    "AttachmentMedia",
    "AttachmentContent",
    "AttachmentTitle",
    "AttachmentDescription",
    "AttachmentActions",
    "AttachmentAction",
    "AttachmentTrigger"
   ],
   "aliases": [],
   "tokens": 991
  },
  {
   "id": "avatar",
   "kind": "component",
   "name": "Avatar",
   "import": "import { Avatar, AvatarImage, AvatarFallback, AvatarGroup, AvatarGroupCount, AvatarBadge } from \"@tecton/react/components/avatar\"",
   "family": "presentation",
   "summary": "A person or an account needs a face: a comment author, an assignee cell, the shell's user menu.",
   "fields": {
    "names": "avatar avatar avatar image avatar fallback avatar group avatar group count avatar badge",
    "useWhen": "A person or an account needs a face: a comment author, an assignee cell, the shell's user menu. The picture may be missing, and initials are an acceptable stand-in. Several people share one row and overlapping them reads better than listing them.",
    "routed": "a round picture of a person or an account",
    "do": "Compose it: `AvatarImage`, then `AvatarFallback`, plus `AvatarBadge` for a presence or status dot. Pick the box with `size=\"sm\" | \"default\" | \"lg\"` (24 / 32 / 40 px); it writes the `data-size` the other parts read. Overlap a row with `AvatarGroup` and close it with `AvatarGroupCount`, which sizes itself from the avatars around it; give `AvatarImage` an `alt` only when the picture carries the identity. Recolour `AvatarBadge` only, and only with a Tecton step: `className=\"bg-green-560\"`."
   },
   "notFor": [
    {
     "need": "a glyph that stands for a thing rather than a person",
     "id": "tecton-icons"
    },
    {
     "need": "a colour value shown as a square",
     "id": "color-swatch"
    },
    {
     "need": "a number pinned to the corner of an icon",
     "id": "count-badge"
    },
    {
     "need": "a person tag the user can select or remove",
     "id": "chip"
    }
   ],
   "related": [
    "count-badge",
    "color-swatch",
    "chip"
   ],
   "exports": [
    "Avatar",
    "AvatarImage",
    "AvatarFallback",
    "AvatarGroup",
    "AvatarGroupCount",
    "AvatarBadge"
   ],
   "aliases": [],
   "tokens": 989
  },
  {
   "id": "background",
   "kind": "component",
   "name": "Background",
   "import": "import { Background, BackgroundEffect, SeismicBackground, ContourBackground, StrataBackground, GridBackground, FlowBackground, WellLogBackground, DrillBackground, HexagonsBackground, PressureBackground, HorizonBackground, TerrainGridBackground, backgroundEffects, backgroundVariants, backgroundTones, backgroundIntensities, backgroundSpeeds } from \"@tecton/react/tecton/background\"",
   "family": "presentation",
   "summary": "A large, otherwise plain surface needs texture: a hero, a sign-in page, an onboarding step, a dashboard header.",
   "fields": {
    "names": "background background background effect seismic background contour background strata background grid background flow background well log background drill background hexagons background pressure background horizon background terrain grid background background effects background variants background tones background intensities background speeds",
    "useWhen": "A large, otherwise plain surface needs texture: a hero, a sign-in page, an onboarding step, a dashboard header. The imagery is decoration — it says nothing a reader has to read, and losing it costs nothing. The surface belongs to this product, so an oil and gas motif reads as the domain rather than as clip art.",
    "routed": "a decorative themed texture behind ordinary content",
    "do": "Render the named effect (`ContourBackground`, `SeismicBackground`, …) as the first child of a `relative isolate overflow-hidden` container, with the content after it. Pick the ink with `tone`, the weight with `intensity` and the cycle with `speed`; on busy screens stay at `intensity=\"low\"`. Let the effect decide when to move: it already freezes under `prefers-reduced-motion`, off screen and in a hidden tab, and hides itself in print and forced colours. `animate={false}` pins it to its static frame. Choose an effect by name with `BackgroundEffect` and the `backgroundEffects`, `backgroundTones`, `backgroundIntensities` and `backgroundSpeeds` lists when the person picks their own. Build a new effect on the shared `Background` layer so it inherits `--bg-ink`, the pausing and the print rules; do not reimplement the layer."
   },
   "notFor": [
    {
     "need": "an interactive map, schematic or 3D view",
     "id": "canvas"
    },
    {
     "need": "a placeholder shape while real content loads",
     "id": "skeleton"
    },
    {
     "need": "an empty state with a message and an action",
     "id": "empty"
    },
    {
     "need": "a picture of real data",
     "id": "chart"
    }
   ],
   "related": [
    "canvas",
    "empty",
    "aspect-ratio"
   ],
   "exports": [
    "Background",
    "BackgroundEffect",
    "SeismicBackground",
    "ContourBackground",
    "StrataBackground",
    "GridBackground",
    "FlowBackground",
    "WellLogBackground",
    "DrillBackground",
    "HexagonsBackground",
    "PressureBackground",
    "HorizonBackground",
    "TerrainGridBackground",
    "backgroundEffects",
    "backgroundVariants",
    "backgroundTones",
    "backgroundIntensities",
    "backgroundSpeeds"
   ],
   "aliases": [],
   "tokens": 1087
  },
  {
   "id": "badge",
   "kind": "component",
   "name": "Badge",
   "import": "import { Badge } from \"@tecton/react/components/badge\"",
   "family": "labels",
   "summary": "A static label names a status, a category or a state: \"Draft\", \"Producing\", \"12 errors\".",
   "fields": {
    "names": "badge badge",
    "useWhen": "A static label names a status, a category or a state: \"Draft\", \"Producing\", \"12 errors\". The label carries a severity: `variant=\"success\" | \"warning\" | \"info\" | \"destructive\"`. The label links to the thing it names, inside a table cell, a list row or a card header.",
    "routed": "a one-word status label inside a row or a header a short static label such as a file type or a count a label that is never selected or removed a label that names a status or a category in words a label with words in it, in the flow of the page a status or category label a status or category label that sits inside a line of text a one-word risk label in a row or a header a word that names a status or a category",
    "do": "Pick meaning with `variant`, weight with `appearance=\"solid\" | \"outline\"`, height with `size=\"default\" | \"md\" | \"lg\"` (20 / 24 / 28 px). Give an icon or a `Spinner` child `data-icon=\"inline-start\"` or `data-icon=\"inline-end\"` so the badge trims its padding on that side. Make it a link with `render={<a href=\"/wells/12\" />}`; there is no `asChild`, and an outer `<a>` gets none of the badge's `[a]:hover:` rules. A category colour that no `variant` carries is the one documented exception: a Tecton palette pair, `className=\"bg-blue-120 text-blue-830\"`, never a stock Tailwind colour. Otherwise `className` is for placement."
   },
   "notFor": [
    {
     "need": "a tag the user can select or remove",
     "id": "chip"
    },
    {
     "need": "a count or dot pinned to the corner of an icon or avatar",
     "id": "count-badge"
    },
    {
     "need": "a confirmation that disappears on its own",
     "id": "sonner"
    },
    {
     "need": "a keyboard key or a shortcut hint",
     "id": "kbd"
    }
   ],
   "related": [
    "chip",
    "count-badge"
   ],
   "exports": [
    "Badge"
   ],
   "aliases": [],
   "tokens": 1047
  },
  {
   "id": "breadcrumb",
   "kind": "component",
   "name": "Breadcrumb",
   "import": "import { Breadcrumb, BreadcrumbList, BreadcrumbItem, BreadcrumbLink, BreadcrumbPage, BreadcrumbSeparator, BreadcrumbEllipsis } from \"@tecton/react/components/breadcrumb\"",
   "family": "navigation",
   "summary": "The path from the application root down to the record on screen, one link per level.",
   "fields": {
    "names": "breadcrumb breadcrumb breadcrumb list breadcrumb item breadcrumb link breadcrumb page breadcrumb separator breadcrumb ellipsis",
    "useWhen": "The path from the application root down to the record on screen, one link per level. The trail is the eyebrow of a page title block, above the `PageHeaderTitle`. A deep path has to collapse in the middle, behind an ellipsis or a menu.",
    "routed": "the trail of parent pages above a page title",
    "do": "Compose `Breadcrumb` > `BreadcrumbList` > `BreadcrumbItem`, and end the trail with `BreadcrumbPage` — the `span` carrying `aria-current=\"page\"`, never a link restyled to look current. Put a `BreadcrumbSeparator` between items: it is the presentational `li` holding the chevron (flipped in right-to-left); pass a child to change the glyph. Give `BreadcrumbLink` an `href`, and mount a routing library's link with `render={<Link to=\"/fields\" />}`. Collapse the middle with `BreadcrumbEllipsis`, or put it in a `DropdownMenuTrigger` when those levels must stay reachable."
   },
   "notFor": [
    {
     "need": "switching between sibling views of the same page",
     "id": "tabs"
    },
    {
     "need": "section tabs under a page title",
     "id": "page-header"
    },
    {
     "need": "moving through the pages of one list",
     "id": "pagination"
    }
   ],
   "related": [
    "page-header",
    "tabs",
    "pagination"
   ],
   "exports": [
    "Breadcrumb",
    "BreadcrumbList",
    "BreadcrumbItem",
    "BreadcrumbLink",
    "BreadcrumbPage",
    "BreadcrumbSeparator",
    "BreadcrumbEllipsis"
   ],
   "aliases": [],
   "tokens": 1017
  },
  {
   "id": "bubble",
   "kind": "component",
   "name": "Bubble",
   "import": "import { Bubble, BubbleGroup, BubbleContent, BubbleReactions } from \"@tecton/react/components/bubble\"",
   "family": "conversation",
   "summary": "The framed surface one message is printed on: chat text, a quoted reply, a short structured answer.",
   "fields": {
    "names": "bubble bubble bubble group bubble content bubble reactions",
    "useWhen": "The framed surface one message is printed on: chat text, a quoted reply, a short structured answer. Assistant prose or rich content must span the row: `variant=\"ghost\"` drops the frame and the 80% cap. The surface itself is pressable: a suggestion, a quick reply, a failed action that opens its error.",
    "routed": "the framed surface the message text is printed on",
    "do": "Always put the text inside `BubbleContent`; every variant styles `*:data-[slot=bubble-content]`, not the root. Pick the role with `variant=\"default\" | \"secondary\" | \"muted\" | \"tinted\" | \"outline\" | \"ghost\" | \"destructive\"`. Set the side with `align=\"end\"` on the `Bubble`, or let the surrounding `Message align=\"end\"` do it. Anchor emoji or quick actions with `BubbleReactions side=\"top\" | \"bottom\"`; give a static row `role=\"img\"` and an `aria-label`. Keep `className` for width and inner layout; the padding, radius and fill belong to the variant."
   },
   "notFor": [
    {
     "need": "the whole turn, with avatar, sender name and message actions",
     "id": "message"
    },
    {
     "need": "a row of an application list, with media, title and actions",
     "id": "item"
    },
    {
     "need": "a file or an image card inside a message or a composer",
     "id": "attachment"
    }
   ],
   "related": [
    "message",
    "attachment",
    "marker"
   ],
   "exports": [
    "Bubble",
    "BubbleGroup",
    "BubbleContent",
    "BubbleReactions"
   ],
   "aliases": [],
   "tokens": 901
  },
  {
   "id": "button",
   "kind": "component",
   "name": "Button",
   "import": "import { Button, buttonVariants } from \"@tecton/react/components/button\"",
   "family": "actions",
   "summary": "Something happens in place: submit, open a dialog, run an action, trigger a menu.",
   "fields": {
    "names": "button button button variants",
    "useWhen": "Something happens in place: submit, open a dialog, run an action, trigger a menu. A real `button` element is the right semantics, and one of the action weights fits: `default`, `secondary`, `outline`, `ghost`, `destructive`, `link`.",
    "routed": "any action on a value other than copying it something that happens in place instead of navigation a control that performs an action rather than holding state",
    "do": "Handle presses with `onClick` and disable with `disabled`; add `focusableWhenDisabled` when a disabled button must stay in the tab order (a pending submit). Pick weight with `variant` and the box with `size`; keep `className` for layout only (`w-full`, `ms-auto`). Mark icons with `data-icon=\"inline-start\" | \"inline-end\"` so the padding adjusts, and give icon-only buttons an `aria-label`. Navigate with `LinkButton` (`@tecton/react/tecton/link`): an `a` with the button look that keeps the link role and uses the `TectonProvider` router. `buttonVariants()` on a plain `a` works too. A `Button` never renders the link itself: with `render={<a />}` it keeps `role=\"button\"`, so the link is announced as a button. A trigger (`DialogTrigger`, `PopoverTrigger`, `DropdownMenuTrigger`) takes the button as `render={<Button variant=\"outline\" />}`; never nest a `Button` inside a trigger."
   },
   "notFor": [
    {
     "need": "a link inside a sentence or a paragraph",
     "id": "link"
    },
    {
     "need": "several related actions behind one control",
     "id": "dropdown-menu"
    },
    {
     "need": "copying a value to the clipboard with feedback",
     "id": "copy-button"
    }
   ],
   "related": [
    "button-group",
    "link"
   ],
   "exports": [
    "Button",
    "buttonVariants"
   ],
   "aliases": [],
   "tokens": 1286
  },
  {
   "id": "button-group",
   "kind": "component",
   "name": "ButtonGroup",
   "import": "import { ButtonGroup, ButtonGroupSeparator, ButtonGroupText, buttonGroupVariants } from \"@tecton/react/components/button-group\"",
   "family": "actions",
   "summary": "Two or more controls belong to one object: a split button, a toolbar cluster, an input with a button.",
   "fields": {
    "names": "button group button group button group separator button group text button group variants",
    "useWhen": "Two or more controls belong to one object: a split button, a toolbar cluster, an input with a button. The controls should read as a single joined shape rather than separate buttons. Text, an `Input`, an `InputGroup` or a `SelectTrigger` has to sit flush against a button.",
    "routed": "a permanent row of page actions actions that must be discoverable on the surface two or three buttons welded into one control buttons that perform actions rather than hold state",
    "do": "Wrap the children and let the group join them; use `orientation=\"vertical\"` for a stacked cluster. Give the group an `aria-label` or `aria-labelledby`; it renders a bare `div` with `role=\"group\"`. Put a `ButtonGroupSeparator` between filled buttons; `variant=\"outline\"` buttons already carry a border. Use `ButtonGroupText` for static text, with its `render` prop when the text must be a `Label`. Nest `ButtonGroup`s to separate clusters; the outer group adds the gap itself."
   },
   "notFor": [
    {
     "need": "options that hold an on or off state",
     "id": "toggle-group"
    },
    {
     "need": "sections of content the user moves between",
     "id": "tabs"
    },
    {
     "need": "a long list of actions behind one trigger",
     "id": "dropdown-menu"
    }
   ],
   "related": [
    "button",
    "toggle-group",
    "dropdown-menu"
   ],
   "exports": [
    "ButtonGroup",
    "ButtonGroupSeparator",
    "ButtonGroupText",
    "buttonGroupVariants"
   ],
   "aliases": [],
   "tokens": 1152
  },
  {
   "id": "calendar",
   "kind": "component",
   "name": "Calendar",
   "import": "import { Calendar, CalendarDayButton } from \"@tecton/react/components/calendar\"",
   "family": "data",
   "summary": "A month grid is the point: choosing a spud date, reading which days are booked, stepping through a schedule.",
   "fields": {
    "names": "calendar calendar calendar day button",
    "useWhen": "A month grid is the point: choosing a spud date, reading which days are booked, stepping through a schedule. The calendar stays on the page — in a `Card`, a panel or a wizard step — rather than hiding behind a trigger. A period is chosen as two dates: `mode=\"range\"` draws the span between them.",
    "routed": "",
    "do": "Pick the selection with `mode=\"single\" | \"multiple\" | \"range\"` and drive it with `selected` / `onSelect`, holding plain `Date` values (a `DateRange` `{ from, to }` for a range). Show more months with `numberOfMonths`, month and year dropdowns with `captionLayout=\"dropdown\"`, week numbers with `showWeekNumber`. Block days with `disabled` — a matcher such as `{ before: new Date() }`, `{ dayOfWeek: [0, 6] }` or a function — so they get the dimmed cell styling. Resize the grid through its variable, `className=\"[--cell-size:--spacing(11)]\"`; never restyle the cells. For a date field, put the `Calendar` in a `PopoverContent` behind a `PopoverTrigger render={<Button variant=\"outline\" />}` inside a `Field`; there is no `DatePicker` component."
   },
   "notFor": [
    {
     "need": "a compact date field that opens a calendar on demand",
     "id": "popover"
    },
    {
     "need": "a year or a month picked in a dense form row",
     "id": "native-select"
    }
   ],
   "related": [
    "popover",
    "field",
    "native-select"
   ],
   "exports": [
    "Calendar",
    "CalendarDayButton"
   ],
   "aliases": [],
   "tokens": 960
  },
  {
   "id": "canvas",
   "kind": "component",
   "name": "Canvas",
   "import": "import { Canvas, CanvasSurface, CanvasOverlay, CanvasToolbar, CanvasToolbarButton, CanvasLegend, CanvasLegendItem, canvasOverlayVariants, canvasToolbarVariants } from \"@tecton/react/tecton/canvas\"",
   "family": "presentation",
   "summary": "A map, a schematic, a seismic section or a 3D view owns the whole work area and the chrome floats over it.",
   "fields": {
    "names": "canvas canvas canvas surface canvas overlay canvas toolbar canvas toolbar button canvas legend canvas legend item canvas overlay variants canvas toolbar variants",
    "useWhen": "A map, a schematic, a seismic section or a 3D view owns the whole work area and the chrome floats over it. The surface is pannable or zoomable, so the controls must not steal the drags that pass between them. The view fills whatever height is left in its column rather than a height the design picked.",
    "routed": "a full-bleed map, schematic or 3D view an interactive map, schematic or 3D view",
    "do": "Compose it: `CanvasSurface` for the engine, then one `CanvasOverlay` per edge or corner holding `CanvasToolbar`s and a `CanvasLegend`. Write the symbology as `CanvasLegendItem`s inside a `CanvasLegend`: it is a list of items, each a decorative swatch followed by its name, and `swatch` takes a CSS colour string (`\"var(--chart-1)\"`) or a node for a pattern. Pin chrome with `position` on `CanvasOverlay` (the eight edges and corners); side and corner overlays stack their children down the column, `top` and `bottom` across. Set a rail's direction with `orientation` on `CanvasToolbar` and give it an `aria-label`; it is a `role=\"toolbar\"` (arrow keys along the rail, stopping at its ends, Tab out of it) and already carries the floating surface, blur and shadow. Fill a rail with `CanvasToolbarButton`s (a ghost icon `Button`; compose a menu or popover trigger through `render`), and give the canvas its height from the column it sits in: a flex-column parent, or `className=\"h-72\"` when it is embedded."
   },
   "notFor": [
    {
     "need": "a titled tool surface with a scrolling body and a pinned footer",
     "id": "panel"
    },
    {
     "need": "a self-contained block of content in a grid",
     "id": "card"
    },
    {
     "need": "the application frame around the work area",
     "id": "app-shell"
    },
    {
     "need": "a decorative themed texture behind ordinary content",
     "id": "background"
    }
   ],
   "related": [
    "panel",
    "app-shell",
    "background"
   ],
   "exports": [
    "Canvas",
    "CanvasSurface",
    "CanvasOverlay",
    "CanvasToolbar",
    "CanvasToolbarButton",
    "CanvasLegend",
    "CanvasLegendItem",
    "canvasOverlayVariants",
    "canvasToolbarVariants"
   ],
   "aliases": [],
   "tokens": 1053
  },
  {
   "id": "card",
   "kind": "component",
   "name": "Card",
   "import": "import { Card, CardHeader, CardTitle, CardDescription, CardAction, CardContent, CardFooter } from \"@tecton/react/components/card\"",
   "family": "surfaces",
   "summary": "A self-contained piece of content stands on its own: a summary, a sign-in form, a media tile.",
   "fields": {
    "names": "card card card header card title card description card action card content card footer",
    "useWhen": "A self-contained piece of content stands on its own: a summary, a sign-in form, a media tile. The card is as tall as its content — it does not scroll and it is not part of the app chrome. It sits in a grid or a stack of siblings that should all look the same.",
    "routed": "a self-contained block of content in a grid a standalone block with its own title, body and footer a heading inside a card a self-contained content block that grows with its text",
    "do": "Compose it: `CardHeader` with `CardTitle`, `CardDescription` and `CardAction`, then `CardContent`, then `CardFooter`. Set density with `size=\"sm\"`, or with the documented `[--card-spacing:--spacing(4)]` on `Card` when a design needs another step. Divide sections with a bare `border-b` on `CardHeader` or `border-t` on `CardFooter`; the card adds the matching padding itself. Bleed content to the edges with `-mx-(--card-spacing)`, so it still lines up with the card inset. Keep `className` to placement and width (`w-full max-w-sm`): the card owns the surface, ring, radius and padding."
   },
   "notFor": [
    {
     "need": "a titled tool surface whose body scrolls and whose footer stays put",
     "id": "panel"
    },
    {
     "need": "one row in a list of records",
     "id": "item"
    },
    {
     "need": "the title block at the top of a page",
     "id": "page-header"
    },
    {
     "need": "rows with sorting, paging and selection",
     "id": "tanstack-table"
    }
   ],
   "related": [
    "panel",
    "item",
    "page-header"
   ],
   "exports": [
    "Card",
    "CardHeader",
    "CardTitle",
    "CardDescription",
    "CardAction",
    "CardContent",
    "CardFooter"
   ],
   "aliases": [],
   "tokens": 912
  },
  {
   "id": "carousel",
   "kind": "component",
   "name": "Carousel",
   "import": "import { Carousel, CarouselContent, CarouselItem, CarouselPrevious, CarouselNext, useCarousel } from \"@tecton/react/components/carousel\"",
   "family": "presentation",
   "summary": "A short, ordered set of equal tiles does not fit and stepping through them is the point: onboarding slides, a gallery, featured records.",
   "fields": {
    "names": "carousel carousel carousel content carousel item carousel previous carousel next use carousel",
    "useWhen": "A short, ordered set of equal tiles does not fit and stepping through them is the point: onboarding slides, a gallery, featured records. Nothing is lost if the reader never reaches the last slide. The set is small enough that every slide can be in the DOM at once.",
    "routed": "",
    "do": "Compose it: `Carousel`, then `CarouselContent` holding `CarouselItem`s, then `CarouselPrevious` and `CarouselNext` as siblings of the content. Pass Embla options on `Carousel` through `opts` (`{ align: \"start\", loop: true }`) and plugins through `plugins`. Switch the axis with `orientation=\"vertical\"`, never with an Embla `axis` option, and set how many slides show with `basis-*` on `CarouselItem`; the `-ms-4` / `ps-4` gutter is already on the parts. Read position and react to moves through `setApi` and the Embla instance; there is no selected-index prop."
   },
   "notFor": [
    {
     "need": "sections the reader chooses by name",
     "id": "tabs"
    },
    {
     "need": "a row of cards the reader scrolls freely",
     "id": "scroll-area"
    }
   ],
   "related": [
    "tabs",
    "scroll-area",
    "aspect-ratio"
   ],
   "exports": [
    "Carousel",
    "CarouselContent",
    "CarouselItem",
    "CarouselPrevious",
    "CarouselNext",
    "useCarousel"
   ],
   "aliases": [],
   "tokens": 951
  },
  {
   "id": "chart",
   "kind": "component",
   "name": "ChartContainer",
   "import": "import { ChartContainer, ChartTooltip, ChartTooltipContent, ChartLegend, ChartLegendContent, ChartStyle } from \"@tecton/react/components/chart\"",
   "family": "data",
   "summary": "Several values are compared over time or across categories: production by month, cost by discipline.",
   "fields": {
    "names": "chart chart container chart tooltip chart tooltip content chart legend chart legend content chart style",
    "useWhen": "Several values are compared over time or across categories: production by month, cost by discipline. The chart itself is Recharts — `BarChart`, `LineChart`, `AreaChart` and `PieChart` go inside `ChartContainer` unchanged. The series need one set of labels, colours and icons that the tooltip and the legend both read: that is the `ChartConfig`.",
    "routed": "a picture of real data the colour key for the series of a chart a series compared over time or across categories",
    "do": "Key the `ChartConfig` by `dataKey`, each entry with `label`, `color` (or `theme: { light, dark }`) and an optional `icon`, and pass it to `ChartContainer`. Take the colours from the theme: `var(--chart-1)` … `var(--chart-5)` are the Tecton accent fills, already paired for light and dark. Paint each series with the variable the container emits — `fill=\"var(--color-desktop)\"`, `stroke=\"var(--color-mobile)\"`. Give the container a size, `className=\"h-64 w-full\"`, since the `ResponsiveContainer` inside measures its parent, and put `accessibilityLayer` on the Recharts chart. Add `ChartTooltip content={<ChartTooltipContent />}` and `ChartLegend content={<ChartLegendContent />}`; both read their labels and swatches from the same config."
   },
   "notFor": [
    {
     "need": "one measured number with a unit and a change",
     "id": "stat"
    },
    {
     "need": "a value read against a fixed scale with named bands",
     "id": "meter"
    }
   ],
   "related": [
    "stat",
    "meter",
    "progress"
   ],
   "exports": [
    "ChartContainer",
    "ChartTooltip",
    "ChartTooltipContent",
    "ChartLegend",
    "ChartLegendContent",
    "ChartStyle"
   ],
   "aliases": [],
   "tokens": 1119
  },
  {
   "id": "checkbox",
   "kind": "component",
   "name": "Checkbox",
   "import": "import { Checkbox } from \"@tecton/react/components/checkbox\"",
   "family": "forms",
   "summary": "An independent yes/no value the user confirms by submitting the form.",
   "fields": {
    "names": "checkbox checkbox",
    "useWhen": "An independent yes/no value the user confirms by submitting the form. Several options from a list may be picked at once. A parent row summarising children that are partly selected (`indeterminate`).",
    "routed": "a term the user ticks as part of submitting a form",
    "do": "Control it with `checked` and `onCheckedChange(checked: boolean)`, or leave it uncontrolled with `defaultChecked`. Give it an `id` and point a `FieldLabel htmlFor` at it inside a `Field orientation=\"horizontal\"`; add `FieldContent` when there is a description. Disable with `disabled` and put `data-disabled` on the `Field` so the label dims with it. Mark errors with `aria-invalid` on the checkbox and `data-invalid` on the `Field`, then render the message in `FieldError`. Submit it with a form through `name` (and `value`); a row-selection column in a `Table` is a `Checkbox` per row driven by your own selection state."
   },
   "notFor": [
    {
     "need": "one choice out of several mutually exclusive options",
     "id": "radio-group"
    },
    {
     "need": "a setting that takes effect the moment it is flipped",
     "id": "switch"
    },
    {
     "need": "a legend and shared description over a list of checkboxes",
     "id": "field"
    }
   ],
   "related": [
    "switch",
    "field"
   ],
   "exports": [
    "Checkbox"
   ],
   "aliases": [],
   "tokens": 810
  },
  {
   "id": "chip",
   "kind": "component",
   "name": "Chip",
   "import": "import { Chip, ChipGroup, ChipList, ChipRemove } from \"@tecton/react/tecton/chip\"",
   "family": "labels",
   "summary": "The user toggles the label: active filters, selected facies, chosen horizons.",
   "fields": {
    "names": "chip chip chip group chip list chip remove",
    "useWhen": "The user toggles the label: active filters, selected facies, chosen horizons. The user removes the label: applied filters, recipients, keywords, attachments. The set is small enough that every chip is on screen at once.",
    "routed": "a tag the user can select or remove from a filter row a person tag the user can select or remove a tag the user can select or remove a tag the user can select or remove",
    "do": "Compose `ChipGroup > ChipList > Chip` and name the group with `aria-label`; the list is the `role=\"grid\"` it names. Give every `Chip` a `value`: it is what `value`, `onValueChange` and `onRemove` hold. Add a `label` when the children are not plain text. Turn on selection with `selectionMode=\"single\" | \"multiple\"` and `value` / `onValueChange` (or `defaultValue`) on the group; `disabled` on a chip or on the whole group. Turn on removal with `onRemove` on the group: every chip gets its remove button, and Delete or Backspace removes the focused one. Take the look from the Badge axes: `variant`, `appearance=\"outline\"`, `size`. `className` is for layout only; style state with `data-[selected]:` (shadcn's `data-selected:` matches only `\"true\"`), `data-disabled`, `:hover` and `:focus-visible`."
   },
   "notFor": [
    {
     "need": "a label that is never selected or removed",
     "id": "badge"
    },
    {
     "need": "a count or dot pinned to the corner of an icon or avatar",
     "id": "count-badge"
    },
    {
     "need": "a confirmation that a tag was removed",
     "id": "sonner"
    }
   ],
   "related": [
    "badge",
    "count-badge"
   ],
   "exports": [
    "Chip",
    "ChipGroup",
    "ChipList",
    "ChipRemove"
   ],
   "aliases": [],
   "tokens": 1095
  },
  {
   "id": "circular-progress",
   "kind": "component",
   "name": "CircularProgress",
   "import": "import { CircularProgress } from \"@tecton/react/tecton/circular-progress\"",
   "family": "progress",
   "summary": "The progress belongs in a compact space: a card corner, a table cell, a tile, a KPI.",
   "fields": {
    "names": "circular progress circular progress",
    "useWhen": "The progress belongs in a compact space: a card corner, a table cell, a tile, a KPI. The number matters as much as the arc: `showValue`, or children such as `7/12`. A ring, not an icon, is the right weight for an indeterminate wait inside a panel or a dialog.",
    "routed": "a ring with the value in the middle a ring with the value in the middle",
    "do": "Always pass `aria-label`; there is no visible label slot. Size with `size=\"xs\" | \"sm\" | \"md\" | \"lg\" | \"xl\"` (16 / 24 / 40 / 64 / 96 px): one prop drives the diameter, the stroke width and the centre type together. Colour with `color=\"default\" | \"foreground\" | \"success\" | \"warning\" | \"error\" | \"info\"`; `default` is the shared `progress` fill. Give the scale with `min` and `max`, shape the text with `format` (`Intl.NumberFormatOptions`), or pass children for custom centre content. Pass `value={null}`, not `value={0}`, while the total is unknown: the ring spins."
   },
   "notFor": [
    {
     "need": "a full-width determinate bar in the flow of a panel",
     "id": "progress"
    },
    {
     "need": "a small busy glyph inside a button or an input",
     "id": "spinner"
    },
    {
     "need": "a segmented readout of risk, complexity or confidence",
     "id": "meter"
    }
   ],
   "related": [
    "progress",
    "spinner",
    "meter"
   ],
   "exports": [
    "CircularProgress"
   ],
   "aliases": [],
   "tokens": 919
  },
  {
   "id": "collapsible",
   "kind": "component",
   "name": "Collapsible",
   "import": "import { Collapsible, CollapsibleTrigger, CollapsibleContent } from \"@tecton/react/components/collapsible\"",
   "family": "layout",
   "summary": "One region shows more detail on demand: advanced settings, a row's sub-fields, a long log.",
   "fields": {
    "names": "collapsible collapsible collapsible trigger collapsible content",
    "useWhen": "One region shows more detail on demand: advanced settings, a row's sub-fields, a long log. The control that toggles it is already on the page and stays where it is. Nothing else collapses with it — a second collapsible next to the first is an `Accordion`.",
    "routed": "one region that opens and closes on its own",
    "do": "Control it with `open` and `onOpenChange` on `Collapsible`, or leave it uncontrolled with `defaultOpen`. Style the trigger with `CollapsibleTrigger render={<Button variant=\"ghost\" />}`; on its own it is an unstyled `button`. Keep the revealed markup inside `CollapsibleContent`: it is the panel the trigger's `aria-expanded` and `aria-controls` refer to. `Collapsible` itself is one unstyled `div`, so its `className` is yours for the layout."
   },
   "notFor": [
    {
     "need": "several stacked sections with one open at a time",
     "id": "accordion"
    },
    {
     "need": "a panel that slides in over the page and is dismissed",
     "id": "sheet"
    },
    {
     "need": "short content anchored to the control that opened it",
     "id": "popover"
    }
   ],
   "related": [
    "accordion",
    "sheet",
    "popover"
   ],
   "exports": [
    "Collapsible",
    "CollapsibleTrigger",
    "CollapsibleContent"
   ],
   "aliases": [],
   "tokens": 1076
  },
  {
   "id": "color-swatch",
   "kind": "component",
   "name": "ColorSwatch",
   "import": "import { ColorSwatch } from \"@tecton/react/tecton/color-swatch\"",
   "family": "data",
   "summary": "The colour *is* the data: a horizon colour, a facies legend, a theme token, a series fill.",
   "fields": {
    "names": "color swatch color swatch",
    "useWhen": "The colour *is* the data: a horizon colour, a facies legend, a theme token, a series fill. The colour needs its name or its value beside it: `label` and `value`. The user has to change it: `onChange` turns the swatch into a button with a preset and hex picker.",
    "routed": "a colour value shown as a square",
    "do": "Pass any CSS colour to `color` — a hex, `rgb()`, `hsl()`, `oklch()`, a keyword or `var(--tecton-color-accent-lime-fill)`; a translucent colour shows over a checkerboard. Size with `size=\"xs\" | \"sm\" | \"md\" | \"lg\" | \"xl\"` (12 to 48 px) and pick the corner with `shape=\"square\" | \"rounded\" | \"circle\"`. Name what the colour stands for with `aria-label` (or a string `label`); the swatch appends the colour's English name itself (\"Sandstone, vibrant orange\"), and `colorName` replaces that name. Make it editable with `onChange`, which receives `#RRGGBB`, and replace the Tecton accent presets with `presets` when the palette is domain-specific; the presets are a radio group, so an arrow key selects as it moves."
   },
   "notFor": [
    {
     "need": "a label that names a status or a category in words",
     "id": "badge"
    },
    {
     "need": "the colour key for the series of a chart",
     "id": "chart"
    },
    {
     "need": "a count or a dot pinned to the corner of an icon or an avatar",
     "id": "count-badge"
    }
   ],
   "related": [
    "badge",
    "chart",
    "tree-view"
   ],
   "exports": [
    "ColorSwatch"
   ],
   "aliases": [],
   "tokens": 841
  },
  {
   "id": "combobox",
   "kind": "component",
   "name": "Combobox",
   "import": "import { Combobox, ComboboxInput, ComboboxContent, ComboboxList, ComboboxItem, ComboboxGroup, ComboboxLabel, ComboboxEmpty, ComboboxSeparator, ComboboxChips, ComboboxChip, ComboboxChipsInput, ComboboxCollection, ComboboxTrigger, ComboboxValue, useComboboxAnchor } from \"@tecton/react/components/combobox\"",
   "family": "selection",
   "summary": "The list is long enough that typing to narrow it beats scrolling a popover.",
   "fields": {
    "names": "combobox combobox combobox input combobox content combobox list combobox item combobox group combobox label combobox empty combobox separator combobox chips combobox chip combobox chips input combobox collection combobox trigger combobox value use combobox anchor",
    "useWhen": "The list is long enough that typing to narrow it beats scrolling a popover. The user picks several values and should see them as removable chips. The field still resolves to values from a known list, not to free text.",
    "routed": "picking a value for a form field typing to narrow a long list a list the user narrows by typing",
    "do": "Pass the options as `items` on `Combobox` and render them with the `ComboboxList` function child: `{(item) => <ComboboxItem key={item} value={item}>{item}</ComboboxItem>}`. Drive the selection with `value` / `defaultValue` / `onValueChange`; for object items add `itemToStringLabel` so filtering and the input text have a string. Put `ComboboxEmpty` inside `ComboboxContent`; it shows only when the filter matches nothing. Name the input: `aria-label` on `ComboboxInput`, or an `id` on it with a `FieldLabel htmlFor` inside a `Field`. For multi-select pass `multiple`, render `ComboboxChips ref={anchor}` holding `ComboboxValue` → `ComboboxChip`s + `ComboboxChipsInput`, and give `ComboboxContent` the same `anchor` from `useComboboxAnchor()`."
   },
   "notFor": [
    {
     "need": "a short fixed list that needs no typing",
     "id": "select"
    },
    {
     "need": "a palette that runs commands instead of setting a value",
     "id": "command"
    }
   ],
   "related": [
    "select",
    "command",
    "native-select"
   ],
   "exports": [
    "Combobox",
    "ComboboxInput",
    "ComboboxContent",
    "ComboboxList",
    "ComboboxItem",
    "ComboboxGroup",
    "ComboboxLabel",
    "ComboboxEmpty",
    "ComboboxSeparator",
    "ComboboxChips",
    "ComboboxChip",
    "ComboboxChipsInput",
    "ComboboxCollection",
    "ComboboxTrigger",
    "ComboboxValue",
    "useComboboxAnchor"
   ],
   "aliases": [],
   "tokens": 1345
  },
  {
   "id": "command",
   "kind": "component",
   "name": "Command",
   "import": "import { Command, CommandDialog, CommandInput, CommandList, CommandEmpty, CommandGroup, CommandItem, CommandSeparator, CommandShortcut } from \"@tecton/react/components/command\"",
   "family": "selection",
   "summary": "The user should reach commands and records by typing, from anywhere in the application.",
   "fields": {
    "names": "command command command dialog command input command list command empty command group command item command separator command shortcut",
    "useWhen": "The user should reach commands and records by typing, from anywhere in the application. The surface is a palette, usually inside `CommandDialog`, rather than a field in a form. The list mixes groups of actions and wants keyboard-shortcut hints beside them.",
    "routed": "a palette of commands and records inside one application a palette that runs commands instead of setting a value a searchable palette of commands",
    "do": "Run the action from `onSelect` on `CommandItem`; it fires on click and on Enter. Put `CommandEmpty` inside `CommandList`: it shows only when the filter matches nothing. Give an item whose children are JSX a `value` (and `keywords` for synonyms), so the filter matches the words, not the markup. Group with `CommandGroup heading=\"…\"`, divide with `CommandSeparator`, hint keys with `CommandShortcut` — a label only, since Tecton binds no keys. Open the palette with `CommandDialog` and its `open` / `onOpenChange` props, wrapping a `Command`."
   },
   "notFor": [
    {
     "need": "picking a value for a form field",
     "id": "combobox"
    },
    {
     "need": "a menu of actions opened from a button",
     "id": "dropdown-menu"
    },
    {
     "need": "a menu opened by right click",
     "id": "context-menu"
    }
   ],
   "related": [
    "combobox",
    "dropdown-menu"
   ],
   "exports": [
    "Command",
    "CommandDialog",
    "CommandInput",
    "CommandList",
    "CommandEmpty",
    "CommandGroup",
    "CommandItem",
    "CommandSeparator",
    "CommandShortcut"
   ],
   "aliases": [],
   "tokens": 830
  },
  {
   "id": "composer",
   "kind": "component",
   "name": "Composer",
   "import": "import { Composer, ComposerField, ComposerInput, ComposerToolbar, ComposerSubmit, ComposerHint, ComposerStatusMessage, ComposerAttachments, ComposerSuggestions, ComposerSuggestion, ComposerCommands, useComposer } from \"@tecton/react/tecton/composer\"",
   "family": "conversation",
   "summary": "The user writes to an assistant or agent: the message box under a transcript.",
   "fields": {
    "names": "composer composer composer field composer input composer toolbar composer submit composer hint composer status message composer attachments composer suggestions composer suggestion composer commands use composer",
    "useWhen": "The user writes to an assistant or agent: the message box under a transcript. A reply streams back and the user must be able to stop it, and keep typing the next message meanwhile. The message carries something besides its text (a selection, a file) that the user can see and remove before sending. The user types slash commands (`/new`, `/summarise`) in the message box and picks one from a list.",
    "routed": "",
    "do": "Pass the chat's `status` (`\"ready\" | \"submitted\" | \"streaming\" | \"error\"`) and `onStop`; `ComposerSubmit` swaps Send for Stop and Escape stops the reply. Keep `ComposerHint` (visible, or `visible={false}`) so the textarea is described by its keys, and `ComposerStatusMessage` for sent, stopped and failed. Pair it with a transcript whose `MessageScrollerContent` has `aria-busy` while a reply streams, so the reply is announced once, complete. Pass the prompts already sent as `history` (oldest first) for ArrowUp and ArrowDown, and use `submitMode=\"mod-enter\"` for long-form input. Put extra context in `ComposerAttachments`, prompts to start from in `ComposerSuggestions`, slash commands in `ComposerCommands` inside `ComposerField`, and your own buttons (`InputGroupButton`, with or without a tooltip) straight into `ComposerToolbar` before `ComposerSubmit`."
   },
   "notFor": [
    {
     "need": "a one-line search or filter box",
     "id": "input-group"
    },
    {
     "need": "a form field for a long description",
     "id": "textarea"
    },
    {
     "need": "a structured question with choices for the user to answer",
     "id": "questionnaire"
    }
   ],
   "related": [
    "message-scroller",
    "message",
    "attachment",
    "questionnaire"
   ],
   "exports": [
    "Composer",
    "ComposerField",
    "ComposerInput",
    "ComposerToolbar",
    "ComposerSubmit",
    "ComposerHint",
    "ComposerStatusMessage",
    "ComposerAttachments",
    "ComposerSuggestions",
    "ComposerSuggestion",
    "ComposerCommands",
    "useComposer"
   ],
   "aliases": [],
   "tokens": 979
  },
  {
   "id": "context-menu",
   "kind": "component",
   "name": "ContextMenu",
   "import": "import { ContextMenu, ContextMenuTrigger, ContextMenuContent, ContextMenuPortal, ContextMenuItem, ContextMenuCheckboxItem, ContextMenuRadioGroup, ContextMenuRadioItem, ContextMenuLabel, ContextMenuGroup, ContextMenuSeparator, ContextMenuShortcut, ContextMenuSub, ContextMenuSubTrigger, ContextMenuSubContent } from \"@tecton/react/components/context-menu\"",
   "family": "actions",
   "summary": "A row, card or canvas object has secondary actions that would clutter the surface.",
   "fields": {
    "names": "context menu context menu context menu trigger context menu content context menu portal context menu item context menu checkbox item context menu radio group context menu radio item context menu label context menu group context menu separator context menu shortcut context menu sub context menu sub trigger context menu sub content",
    "useWhen": "A row, card or canvas object has secondary actions that would clutter the surface. Those actions are reachable another way too; the context menu is an accelerator, not the only route. The gesture should be the platform's own: right click, Control-click, long press.",
    "routed": "a menu opened by right click a menu opened by right click on the content",
    "do": "Compose `ContextMenu` (the root) > `ContextMenuTrigger` + `ContextMenuContent`; the trigger is the `div` area that listens for the gesture, so put the target's content (or `render` its element) there. Run actions from `onClick` on `ContextMenuItem`. Build toggles with `ContextMenuCheckboxItem` (`checked` / `onCheckedChange`) and exclusive choices with `ContextMenuRadioGroup` (`value` / `onValueChange`) + `ContextMenuRadioItem`. Mark the dangerous entry with `variant=\"destructive\"`; nest with `ContextMenuSub`, `ContextMenuSubTrigger` and `ContextMenuSubContent`."
   },
   "notFor": [
    {
     "need": "a menu opened from a visible button",
     "id": "dropdown-menu"
    },
    {
     "need": "actions that must be discoverable on the surface",
     "id": "button-group"
    },
    {
     "need": "a bar of actions for the current selection",
     "id": "action-bar"
    }
   ],
   "related": [
    "dropdown-menu",
    "action-bar"
   ],
   "exports": [
    "ContextMenu",
    "ContextMenuTrigger",
    "ContextMenuContent",
    "ContextMenuPortal",
    "ContextMenuItem",
    "ContextMenuCheckboxItem",
    "ContextMenuRadioGroup",
    "ContextMenuRadioItem",
    "ContextMenuLabel",
    "ContextMenuGroup",
    "ContextMenuSeparator",
    "ContextMenuShortcut",
    "ContextMenuSub",
    "ContextMenuSubTrigger",
    "ContextMenuSubContent"
   ],
   "aliases": [],
   "tokens": 1004
  },
  {
   "id": "copy-button",
   "kind": "component",
   "name": "CopyButton",
   "import": "import { CopyButton } from \"@tecton/react/tecton/copy-button\"",
   "family": "actions",
   "summary": "A value on screen is meant to be pasted elsewhere: an identifier, a URL, a snippet.",
   "fields": {
    "names": "copy button copy button",
    "useWhen": "A value on screen is meant to be pasted elsewhere: an identifier, a URL, a snippet. The user needs the copy confirmed in place, without a toast or a dialog. The control sits next to the value, usually as a quiet icon button.",
    "routed": "copying a value to the clipboard with feedback",
    "do": "Pass the text as `value` and let the component own the click handler. Leave it icon-only for a quiet affordance, or pass text `children` for a prominent labelled one. Set weight with `variant` (it defaults to `ghost`) and hold time with `timeout`. React to a successful copy with `onCopied`, and to a refused one (insecure context, denied permission) with `onError`, not by wrapping the button in your own handler. Let the component name itself: with no children it sets `aria-label` to \"Copy\", then \"Copied\" or \"Copy failed\"; either outcome is also announced through a polite live region."
   },
   "notFor": [
    {
     "need": "any action on a value other than copying it",
     "id": "button"
    },
    {
     "need": "a confirmation that appears away from the control",
     "id": "sonner"
    }
   ],
   "related": [
    "button"
   ],
   "exports": [
    "CopyButton"
   ],
   "aliases": [],
   "tokens": 714
  },
  {
   "id": "count-badge",
   "kind": "component",
   "name": "CountBadge",
   "import": "import { CountBadge } from \"@tecton/react/tecton/count-badge\"",
   "family": "labels",
   "summary": "A number belongs to an icon button, an avatar or a tab: unread messages, queued jobs, open comments.",
   "fields": {
    "names": "count badge count badge",
    "useWhen": "A number belongs to an icon button, an avatar or a tab: unread messages, queued jobs, open comments. A presence or status dot belongs to an avatar or a tab: `variant=\"dot\"`. The number decorates the control; it is not a label the user reads on its own.",
    "routed": "a number pinned to the corner of an icon a count or dot pinned to the corner of an icon or avatar a count or dot pinned to the corner of an icon or avatar a count or a dot pinned to the corner of an icon or an avatar",
    "do": "Wrap the control — `CountBadge` renders the positioned anchor and the badge, and the control stays its child. Let the badge hide itself: `count={0}` renders nothing unless `showZero`, and `invisible` hides it while keeping the anchor. Pick `color` — `default`, `secondary`, `destructive`, `success`, `warning`, `info`, the same names as Badge's `variant` — `anchor` (`top-right` … `bottom-left`) and `max` (counts above it render as `99+`). Put the number in the control's accessible name — `aria-label=\"Messages, 4 unread\"` — because the badge is decorative: it renders `aria-hidden`, so assistive tech never hears the number anywhere else."
   },
   "notFor": [
    {
     "need": "a label with words in it, in the flow of the page",
     "id": "badge"
    },
    {
     "need": "a tag the user can select or remove",
     "id": "chip"
    },
    {
     "need": "a busy indicator on an icon button",
     "id": "spinner"
    }
   ],
   "related": [
    "badge",
    "chip"
   ],
   "exports": [
    "CountBadge"
   ],
   "aliases": [],
   "tokens": 858
  },
  {
   "id": "dialog",
   "kind": "component",
   "name": "Dialog",
   "import": "import { Dialog, DialogTrigger, DialogContent, DialogClose, DialogHeader, DialogTitle, DialogDescription, DialogFooter, DialogOverlay, DialogPortal } from \"@tecton/react/components/dialog\"",
   "family": "overlays",
   "summary": "A decision or a short form — editing a record's fields, renaming it — must be finished before the page continues.",
   "fields": {
    "names": "dialog dialog dialog trigger dialog content dialog close dialog header dialog title dialog description dialog footer dialog overlay dialog portal",
    "useWhen": "A decision or a short form — editing a record's fields, renaming it — must be finished before the page continues. The task deserves the whole screen: everything behind it goes inert. The user may abandon it — Escape and the backdrop both cancel.",
    "routed": "a form, or a decision the user may walk away from a modal decision or a short form on the desktop a decision that must block the rest of the page a decision that must be finished before anything else",
    "do": "Compose `Dialog` (the root, no element) > `DialogTrigger render={<Button />}` + `DialogContent`; the content portals itself and draws the overlay. Control it with `open` / `onOpenChange` on `Dialog`; `defaultOpen` for an uncontrolled start. Give every dialog a `DialogTitle` inside `DialogHeader`: it is the dialog's accessible name. Close from the footer with `DialogClose render={<Button variant=\"outline\" />}`, and pass `disablePointerDismissal` on `Dialog` with `showCloseButton={false}` on `DialogContent` when the user must pick a footer action. Keep `className` on `DialogContent` to width and layout (`sm:max-w-lg`): it owns the surface, radius and padding."
   },
   "notFor": [
    {
     "need": "a confirmation before a destructive action",
     "id": "alert-dialog"
    },
    {
     "need": "a side panel that keeps the page visible behind it",
     "id": "sheet"
    },
    {
     "need": "a bottom sheet the user drags with a thumb",
     "id": "drawer"
    }
   ],
   "related": [
    "alert-dialog",
    "sheet",
    "popover"
   ],
   "exports": [
    "Dialog",
    "DialogTrigger",
    "DialogContent",
    "DialogClose",
    "DialogHeader",
    "DialogTitle",
    "DialogDescription",
    "DialogFooter",
    "DialogOverlay",
    "DialogPortal"
   ],
   "aliases": [],
   "tokens": 1086
  },
  {
   "id": "drawer",
   "kind": "component",
   "name": "Drawer",
   "import": "import { Drawer, DrawerTrigger, DrawerContent, DrawerHeader, DrawerTitle, DrawerDescription, DrawerFooter, DrawerClose, DrawerPortal, DrawerOverlay, DrawerSwipeHandle } from \"@tecton/react/components/drawer\"",
   "family": "overlays",
   "summary": "A touch screen: the panel is dragged in and flicked away with a thumb.",
   "fields": {
    "names": "drawer drawer drawer trigger drawer content drawer header drawer title drawer description drawer footer drawer close drawer portal drawer overlay drawer swipe handle",
    "useWhen": "A touch screen: the panel is dragged in and flicked away with a thumb. The content should be able to rest at a partial height (`snapPoints`) before it is opened fully. A responsive pair, where the same content is a `Dialog` on the desktop and a drawer on a phone.",
    "routed": "a bottom sheet the user drags with a thumb a bottom sheet the user drags with a thumb on touch",
    "do": "Control it like every Tecton overlay, with `open`, `defaultOpen` and `onOpenChange` on `Drawer`; `disablePointerDismissal` keeps unsaved input from being flicked away by a backdrop press. Pass the trigger and close elements through `render`, not as children: `<DrawerTrigger render={<Button variant=\"outline\" />}>Open</DrawerTrigger>`. Pick the edge with `swipeDirection=\"down\" | \"up\" | \"left\" | \"right\"` and add the grab handle with `showSwipeHandle`, both on `Drawer`. Make the scrolling region a flex item — `<div className=\"flex-1 overflow-y-auto p-4\">` — because `h-full` does not resolve inside a content-sized drawer."
   },
   "notFor": [
    {
     "need": "a side panel on a pointer-driven screen",
     "id": "sheet"
    },
    {
     "need": "a modal decision or a short form on the desktop",
     "id": "dialog"
    },
    {
     "need": "a confirmation of a destructive action",
     "id": "alert-dialog"
    }
   ],
   "related": [
    "sheet",
    "dialog"
   ],
   "exports": [
    "Drawer",
    "DrawerTrigger",
    "DrawerContent",
    "DrawerHeader",
    "DrawerTitle",
    "DrawerDescription",
    "DrawerFooter",
    "DrawerClose",
    "DrawerPortal",
    "DrawerOverlay",
    "DrawerSwipeHandle"
   ],
   "aliases": [],
   "tokens": 911
  },
  {
   "id": "dropdown-menu",
   "kind": "component",
   "name": "DropdownMenu",
   "import": "import { DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuPortal, DropdownMenuGroup, DropdownMenuLabel, DropdownMenuItem, DropdownMenuCheckboxItem, DropdownMenuRadioGroup, DropdownMenuRadioItem, DropdownMenuSeparator, DropdownMenuShortcut, DropdownMenuSub, DropdownMenuSubTrigger, DropdownMenuSubContent } from \"@tecton/react/components/dropdown-menu\"",
   "family": "actions",
   "summary": "A button opens a short list of actions: row actions, an account menu, a \"more\" overflow.",
   "fields": {
    "names": "dropdown menu dropdown menu dropdown menu trigger dropdown menu content dropdown menu portal dropdown menu group dropdown menu label dropdown menu item dropdown menu checkbox item dropdown menu radio group dropdown menu radio item dropdown menu separator dropdown menu shortcut dropdown menu sub dropdown menu sub trigger dropdown menu sub content",
    "useWhen": "A button opens a short list of actions: row actions, an account menu, a \"more\" overflow. Some of those entries are toggles or one exclusive choice, shown with check marks. The list wants submenus, shortcut hints, or a destructive entry at the end.",
    "routed": "a list of actions behind a single button a short list of actions opened from a header button a long list of actions behind one trigger several related actions behind one control a menu of actions opened from a button a menu opened from a visible button every action behind one button from the start a menu of actions rather than a value",
    "do": "Compose `DropdownMenu` (the root) > `DropdownMenuTrigger render={<Button variant=\"ghost\" size=\"icon\" />}` + `DropdownMenuContent`. Run actions from `onClick` on `DropdownMenuItem`; the menu closes after it. Show toggles with `DropdownMenuCheckboxItem` (`checked` / `onCheckedChange`) and one exclusive choice with `DropdownMenuRadioGroup` (`value` / `onValueChange`) holding `DropdownMenuRadioItem value=\"…\"`. Title groups with `DropdownMenuLabel` inside a `DropdownMenuGroup`, divide with `DropdownMenuSeparator`, hint keys with `DropdownMenuShortcut` (a label only — Tecton binds no keys). Place with `side` / `align` on `DropdownMenuContent` and nest with `DropdownMenuSub`, `DropdownMenuSubTrigger` and `DropdownMenuSubContent`."
   },
   "notFor": [
    {
     "need": "a menu opened by right click on the content",
     "id": "context-menu"
    },
    {
     "need": "choosing a value for a form field",
     "id": "select"
    },
    {
     "need": "a searchable palette of commands",
     "id": "command"
    }
   ],
   "related": [
    "context-menu",
    "button",
    "select"
   ],
   "exports": [
    "DropdownMenu",
    "DropdownMenuTrigger",
    "DropdownMenuContent",
    "DropdownMenuPortal",
    "DropdownMenuGroup",
    "DropdownMenuLabel",
    "DropdownMenuItem",
    "DropdownMenuCheckboxItem",
    "DropdownMenuRadioGroup",
    "DropdownMenuRadioItem",
    "DropdownMenuSeparator",
    "DropdownMenuShortcut",
    "DropdownMenuSub",
    "DropdownMenuSubTrigger",
    "DropdownMenuSubContent"
   ],
   "aliases": [],
   "tokens": 1042
  },
  {
   "id": "empty",
   "kind": "component",
   "name": "Empty",
   "import": "import { Empty, EmptyHeader, EmptyMedia, EmptyTitle, EmptyDescription, EmptyContent } from \"@tecton/react/components/empty\"",
   "family": "feedback",
   "summary": "A list, table, panel or search result has nothing to show, and the user needs to know why and what to do next.",
   "fields": {
    "names": "empty empty empty header empty media empty title empty description empty content",
    "useWhen": "A list, table, panel or search result has nothing to show, and the user needs to know why and what to do next. A feature has not been used yet: no projects, no wells, no saved views. One long operation owns the whole region and there is nothing else to show (`EmptyMedia` holding a `Spinner`).",
    "routed": "the state of a list or a page that has nothing in it an empty state with a message and an action the state when there is nothing to load the state of a list that has nothing in it",
    "do": "Compose `Empty > EmptyHeader > (EmptyMedia, EmptyTitle, EmptyDescription)` and put the actions in `EmptyContent`. Give an icon its tile with `EmptyMedia variant=\"icon\"`; an avatar or an illustration uses the default variant. Add the dashed outline with the `border` utility — the component already carries `border-dashed`. Name the next step: one primary `Button` in `EmptyContent`, at most one secondary beside it. `className` is for the box (`w-full`, `min-h-64`, `border`), not for the type or the colours."
   },
   "notFor": [
    {
     "need": "a message about something that failed on a page that does have content",
     "id": "alert"
    },
    {
     "need": "a placeholder while the content is still loading",
     "id": "skeleton"
    },
    {
     "need": "a confirmation that disappears on its own",
     "id": "sonner"
    }
   ],
   "related": [
    "alert",
    "skeleton"
   ],
   "exports": [
    "Empty",
    "EmptyHeader",
    "EmptyMedia",
    "EmptyTitle",
    "EmptyDescription",
    "EmptyContent"
   ],
   "aliases": [],
   "tokens": 904
  },
  {
   "id": "field",
   "kind": "component",
   "name": "Field",
   "import": "import { Field, FieldContent, FieldDescription, FieldError, FieldGroup, FieldLabel, FieldLegend, FieldSeparator, FieldSet, FieldTitle } from \"@tecton/react/components/field\"",
   "family": "forms",
   "summary": "Any control needs a label: `Input`, `Textarea`, `Select`, `Checkbox`, `Switch`, `Slider`.",
   "fields": {
    "names": "field field field content field description field error field group field label field legend field separator field set field title",
    "useWhen": "Any control needs a label: `Input`, `Textarea`, `Select`, `Checkbox`, `Switch`, `Slider`. The control needs helper text, a validation message, or both. Related controls need one legend (`FieldSet` + `FieldLegend`) or even spacing (`FieldGroup`).",
    "routed": "a message about a single form field a legend and shared description over a list of checkboxes the label, description and error message around the group the label, description and error message around the code the label, description and error message around the control the label of a control that has a description or an error the caption over a group of related controls a heading beside a control that is already inside a label one labelled control with a description and an error, inside a form the title, description and layout around the control the label, description and layout around the control the label, description and error message around the control",
    "do": "Give the control an `id` and point `FieldLabel htmlFor` at it; `Field` renders `role=\"group\"` and associates nothing. For a `Select` the `id` goes on `SelectTrigger`, which renders the button; for a `Combobox`, on `ComboboxInput`. Choose the layout with `orientation=\"vertical\" | \"horizontal\" | \"responsive\"`, and add `FieldContent` when the label and description sit beside the control. Put state on `Field` as data attributes: `data-invalid` when the value is rejected, `data-disabled` next to a disabled control. Write helper text as `FieldDescription` and the message as `FieldError` — children, or `errors={field.state.meta.errors}` for a validator's issue list."
   },
   "notFor": [
    {
     "need": "a label for a control that stands on its own, outside a field",
     "id": "label"
    },
    {
     "need": "the single-line text control itself",
     "id": "input"
    },
    {
     "need": "form state, validation and typed error objects",
     "id": "tanstack-form"
    }
   ],
   "related": [
    "input",
    "textarea",
    "label"
   ],
   "exports": [
    "Field",
    "FieldContent",
    "FieldDescription",
    "FieldError",
    "FieldGroup",
    "FieldLabel",
    "FieldLegend",
    "FieldSeparator",
    "FieldSet",
    "FieldTitle"
   ],
   "aliases": [],
   "tokens": 1095
  },
  {
   "id": "hover-card",
   "kind": "component",
   "name": "HoverCard",
   "import": "import { HoverCard, HoverCardTrigger, HoverCardContent } from \"@tecton/react/components/hover-card\"",
   "family": "overlays",
   "summary": "A link or a name in running text has more behind it: a person, a well, a document.",
   "fields": {
    "names": "hover card hover card hover card trigger hover card content",
    "useWhen": "A link or a name in running text has more behind it: a person, a well, a document. The extra detail is a courtesy — the user can reach the same content by following the link. It is a preview, not a control panel: nothing inside it is the only way to do something.",
    "routed": "a preview that appears when the pointer rests on a link a preview of what is behind a link",
    "do": "Compose `HoverCard` (the root) > `HoverCardTrigger` + `HoverCardContent`; the trigger renders an `a`, so give it the `href` it previews. Tune the timing with `delay` and `closeDelay` on `HoverCardTrigger` — the defaults are 600 ms and 300 ms. Position with `side` / `align` on `HoverCardContent`, and set width with `className` (`w-72`) only. Keep the trigger reachable by keyboard — a real link — so focus can open the card."
   },
   "notFor": [
    {
     "need": "a short label on a control",
     "id": "tooltip"
    },
    {
     "need": "content the user opens deliberately and works in",
     "id": "popover"
    },
    {
     "need": "a row of a list with media, title and actions",
     "id": "item"
    }
   ],
   "related": [
    "tooltip",
    "popover"
   ],
   "exports": [
    "HoverCard",
    "HoverCardTrigger",
    "HoverCardContent"
   ],
   "aliases": [],
   "tokens": 781
  },
  {
   "id": "input",
   "kind": "component",
   "name": "Input",
   "import": "import { Input } from \"@tecton/react/components/input\"",
   "family": "forms",
   "summary": "A single line of text: name, email, password, number, date, file.",
   "fields": {
    "names": "input input",
    "useWhen": "A single line of text: name, email, password, number, date, file. The value is free-form; a fixed set of options belongs in a picker instead. Inside a `Field`, so the control arrives with a label and room for an error.",
    "routed": "the single-line text control itself a plain field with nothing attached to it a password, a token or any other free-form value an exact number the user types, with units or validation a single line of text",
    "do": "Pick the surface with `variant=\"outline\" | \"filled\" | \"text\"` — outline in forms, filled in dense panels, text for inline editing. Wire the label by hand: `<FieldLabel htmlFor=\"x\">` plus `<Input id=\"x\">`. Nothing associates them implicitly. `Input` is a real `<input>`: use the DOM props `value`, `onChange(event)`, `disabled`, `required`, `type` and `aria-invalid`. Mirror the state on the `Field` — `data-disabled` beside `disabled`, `data-invalid` beside `aria-invalid` — and keep `className` to layout (`w-full`, `col-span-2`)."
   },
   "notFor": [
    {
     "need": "more than one line of free text",
     "id": "textarea"
    },
    {
     "need": "an icon, a prefix or a button inside the field",
     "id": "input-group"
    },
    {
     "need": "the label, description and error message around the control",
     "id": "field"
    }
   ],
   "related": [
    "field",
    "input-group",
    "textarea"
   ],
   "exports": [
    "Input"
   ],
   "aliases": [],
   "tokens": 1043
  },
  {
   "id": "input-group",
   "kind": "component",
   "name": "InputGroup",
   "import": "import { InputGroup, InputGroupAddon, InputGroupButton, InputGroupInput, InputGroupText, InputGroupTextarea } from \"@tecton/react/components/input-group\"",
   "family": "forms",
   "summary": "An icon, unit, hint or status belongs inside the field's border: search, currency, `https://`.",
   "fields": {
    "names": "input group input group input group addon input group button input group input input group text input group textarea",
    "useWhen": "An icon, unit, hint or status belongs inside the field's border: search, currency, `https://`. A button acts on the value in place: clear, copy, reveal, remove, send. A textarea needs a toolbar or a footer row that shares its border and focus ring.",
    "routed": "a one-line search or filter box a field with an icon or a button attached an icon, a prefix or a button inside the field a toolbar, a counter or a send button attached to the box",
    "do": "Compose `InputGroup` > `InputGroupInput` or `InputGroupTextarea`, then the addons; the group owns the border, ring and height. Always place `InputGroupAddon` **after** the control in the DOM and position it with `align`: `inline-start` / `inline-end` beside an input, `block-start` / `block-end` above or below a textarea. Put buttons in `InputGroupButton` (`size=\"xs\" | \"icon-xs\" | \"sm\" | \"icon-sm\"`, `onClick`, `aria-label` when icon-only), static copy in `InputGroupText`, and mark errors with `aria-invalid` on the control."
   },
   "notFor": [
    {
     "need": "a plain field with nothing attached to it",
     "id": "input"
    },
    {
     "need": "a verification code split into separate boxes",
     "id": "input-otp"
    },
    {
     "need": "the label, description and error message around the group",
     "id": "field"
    }
   ],
   "related": [
    "input",
    "textarea",
    "field"
   ],
   "exports": [
    "InputGroup",
    "InputGroupAddon",
    "InputGroupButton",
    "InputGroupInput",
    "InputGroupText",
    "InputGroupTextarea"
   ],
   "aliases": [],
   "tokens": 819
  },
  {
   "id": "input-otp",
   "kind": "component",
   "name": "InputOTP",
   "import": "import { InputOTP, InputOTPGroup, InputOTPSlot, InputOTPSeparator } from \"@tecton/react/components/input-otp\"",
   "family": "forms",
   "summary": "A one-time code from email, SMS or an authenticator app.",
   "fields": {
    "names": "input otp input otp input otpgroup input otpslot input otpseparator",
    "useWhen": "A one-time code from email, SMS or an authenticator app. A short fixed-length PIN the user reads from somewhere and types once. The value has a known length and every character deserves its own box.",
    "routed": "a verification code split into separate boxes",
    "do": "Set `maxLength` to the number of `InputOTPSlot` children, and give each slot its absolute `index` across every group. Control it with `value` and `onChange(value: string)`, or leave it uncontrolled with `defaultValue`. Restrict the alphabet with `pattern={REGEXP_ONLY_DIGITS}` or `REGEXP_ONLY_DIGITS_AND_CHARS` from `input-otp`. Split long codes with `InputOTPGroup` and `InputOTPSeparator`, and mark errors with `aria-invalid` on each `InputOTPSlot`. Wrap it in a `Field` with a `FieldLabel htmlFor` pointing at the `InputOTP` id, and disable with `disabled`."
   },
   "notFor": [
    {
     "need": "a password, a token or any other free-form value",
     "id": "input"
    },
    {
     "need": "a field with an icon or a button attached",
     "id": "input-group"
    },
    {
     "need": "the label, description and error message around the code",
     "id": "field"
    }
   ],
   "related": [
    "input",
    "field"
   ],
   "exports": [
    "InputOTP",
    "InputOTPGroup",
    "InputOTPSlot",
    "InputOTPSeparator"
   ],
   "aliases": [],
   "tokens": 628
  },
  {
   "id": "item",
   "kind": "component",
   "name": "Item",
   "import": "import { Item, ItemGroup, ItemSeparator, ItemHeader, ItemMedia, ItemContent, ItemTitle, ItemDescription, ItemActions, ItemFooter } from \"@tecton/react/components/item\"",
   "family": "surfaces",
   "summary": "One row of a list: a file, a person, a well, a setting with a switch beside it.",
   "fields": {
    "names": "item item item group item separator item header item media item content item title item description item actions item footer",
    "useWhen": "One row of a list: a file, a person, a well, a setting with a switch beside it. The row has a shape — media, then title and description, then actions. A stack of rows should look and space consistently: wrap them in `ItemGroup`.",
    "routed": "a row of a file browser or a settings list a row of an application list, with media, title and actions one row in a list of records a row of a list with media, title and actions a row of an application list, with media, title and actions a divider between the rows of a list a list of rows with media, a title and actions rather than columns a flat list of rows with media, a title and actions",
    "do": "Compose it: `ItemMedia`, then `ItemContent` with `ItemTitle` and `ItemDescription`, then `ItemActions`. Make the whole row a link with `render={<a href=\"/wells/34-10-a-12\" />}` (or the router's `Link`); the `[a]:` rules give it the hover and focus states. Choose the look with `variant=\"default\" | \"outline\" | \"muted\"` and the density with `size=\"default\" | \"sm\" | \"xs\"`. Use `ItemMedia variant=\"icon\"` for a glyph and `variant=\"image\"` for a thumbnail; an `Avatar` goes in the plain `ItemMedia`. Separate rows inside an `ItemGroup` with `ItemSeparator`, never with a border class on the row."
   },
   "notFor": [
    {
     "need": "a standalone block with its own title, body and footer",
     "id": "card"
    },
    {
     "need": "rows with columns, sorting, paging and selection",
     "id": "tanstack-table"
    },
    {
     "need": "the trailing actions of a panel header",
     "id": "panel"
    }
   ],
   "related": [
    "card",
    "panel",
    "separator"
   ],
   "exports": [
    "Item",
    "ItemGroup",
    "ItemSeparator",
    "ItemHeader",
    "ItemMedia",
    "ItemContent",
    "ItemTitle",
    "ItemDescription",
    "ItemActions",
    "ItemFooter"
   ],
   "aliases": [],
   "tokens": 956
  },
  {
   "id": "kbd",
   "kind": "component",
   "name": "Kbd",
   "import": "import { Kbd, KbdGroup } from \"@tecton/react/components/kbd\"",
   "family": "labels",
   "summary": "Naming the key that triggers something: a shortcut hint in a menu item, a tooltip, a button or an input group addon.",
   "fields": {
    "names": "kbd kbd kbd group",
    "useWhen": "Naming the key that triggers something: a shortcut hint in a menu item, a tooltip, a button or an input group addon. Showing a combination or a sequence: `KbdGroup` around several `Kbd`. Documenting a shortcut inline in a sentence.",
    "routed": "a keyboard key or a shortcut hint a keyboard key or a shortcut hint",
    "do": "One key per `Kbd`, wrapped in a `KbdGroup` when there is more than one; a combination that reads as a single token (`Ctrl + B`) may stay in one `Kbd`. Inside a `Button` or an `InputGroupAddon`, add `data-icon=\"inline-end\"` (or `inline-start`) so the control trims its padding on that side. Inside a `TooltipContent` leave it alone: `Kbd` already inverts to the tooltip surface through its `in-data-[slot=tooltip-content]` rules. `className` is for nudging placement; the muted surface and the 20 px box belong to the component. A `Kbd` only shows a key: Tecton binds no shortcuts, so show one only for a key the application handles itself."
   },
   "notFor": [
    {
     "need": "a status or category label",
     "id": "badge"
    },
    {
     "need": "a glyph for an action rather than a key",
     "id": "lucide"
    }
   ],
   "related": [
    "badge"
   ],
   "exports": [
    "Kbd",
    "KbdGroup"
   ],
   "aliases": [],
   "tokens": 748
  },
  {
   "id": "label",
   "kind": "component",
   "name": "Label",
   "import": "import { Label } from \"@tecton/react/components/label\"",
   "family": "forms",
   "summary": "A control sits on its own — a toolbar filter, a preview switch — with no `Field` around it.",
   "fields": {
    "names": "label label",
    "useWhen": "A control sits on its own — a toolbar filter, a preview switch — with no `Field` around it. A caption sits beside a value in a header row and still has to point at a control. Something that is not a Tecton form control needs the same label type and disabled behaviour.",
    "routed": "a label for a control that stands on its own, outside a field",
    "do": "Pair `htmlFor` with the control's `id`; `Label` is a plain `<label>` and associates nothing by itself. For a `Select` the `id` goes on `SelectTrigger`; a `Slider` has no labelable element, so give the `Label` an `id` and pass it as the slider's `aria-labelledby`. Reach for `FieldLabel` the moment the control gains a description, an error or a `Field` wrapper. Keep `className` to layout; the size, weight and the disabled dimming belong to the component."
   },
   "notFor": [
    {
     "need": "the label of a control that has a description or an error",
     "id": "field"
    },
    {
     "need": "the caption over a group of related controls",
     "id": "field"
    },
    {
     "need": "a heading beside a control that is already inside a label",
     "id": "field"
    }
   ],
   "related": [
    "field"
   ],
   "exports": [
    "Label"
   ],
   "aliases": [],
   "tokens": 660
  },
  {
   "id": "link",
   "kind": "component",
   "name": "Link",
   "import": "import { Link, LinkButton, linkVariants } from \"@tecton/react/tecton/link\"",
   "family": "actions",
   "summary": "A word or phrase inside running text navigates somewhere: a well, a report, an external register (`Link`).",
   "fields": {
    "names": "link link link button link variants",
    "useWhen": "A word or phrase inside running text navigates somewhere: a well, a report, an external register (`Link`). A navigation target should look like a button: \"Open well\", \"Back to wells\" (`LinkButton`). The destination leaves the application and needs the external marker.",
    "routed": "a link inside a sentence or a paragraph a link inside a sentence a link that navigates with the router",
    "do": "Always pass `href`: both render a real `<a>`, so new-tab, copy-address and the status bar work. Choose colour with `variant=\"default\" | \"primary\" | \"muted\" | \"subtle\"`; leave `size=\"inherit\"` inside prose. `LinkButton` takes the `Button` `variant` and `size`. Mount `TectonProvider` with the router's `navigate` and `useHref`: plain clicks then navigate without a page load, ⌘/Ctrl/Shift-clicks and `target=\"_blank\"` still go to the browser. Router options go in `navigateOptions`. Reach outside the app with `external`, and disable with `disabled` (no `href`, not focusable, `aria-disabled`)."
   },
   "notFor": [
    {
     "need": "something that happens in place instead of navigation",
     "id": "button"
    },
    {
     "need": "the trail of parent pages above a page title",
     "id": "breadcrumb"
    }
   ],
   "related": [
    "button",
    "link",
    "provider"
   ],
   "exports": [
    "Link",
    "LinkButton",
    "linkVariants"
   ],
   "aliases": [],
   "tokens": 907
  },
  {
   "id": "marker",
   "kind": "component",
   "name": "Marker",
   "import": "import { Marker, MarkerIcon, MarkerContent, markerVariants } from \"@tecton/react/components/marker\"",
   "family": "conversation",
   "summary": "A full-width row between turns: \"Thinking...\", \"Explored 4 files\", \"Marcus joined the chat\".",
   "fields": {
    "names": "marker marker marker icon marker content marker variants",
    "useWhen": "A full-width row between turns: \"Thinking...\", \"Explored 4 files\", \"Marcus joined the chat\". A labelled divider inside a transcript: a date, a section break, an unread line. The row leads back to what the agent touched: a file, a diff, a pull request.",
    "routed": "a status or system line between turns",
    "do": "Compose it: `MarkerIcon` for the glyph, `MarkerContent` for the text, which carries the meaning on its own. Pick the layout with `variant=\"default\" | \"border\" | \"separator\"`; `separator` draws its own rules to each side. Announce work in progress with `role=\"status\"` and a `Spinner` inside `MarkerIcon`. Shimmer streaming text with `className=\"shimmer\"` on `MarkerContent`. Make it interactive with `render={<a href={url} />}`, and wrap it in a `MessageScrollerItem` to anchor it."
   },
   "notFor": [
    {
     "need": "a status or category label that sits inside a line of text",
     "id": "badge"
    },
    {
     "need": "a keyboard key or a shortcut hint",
     "id": "kbd"
    },
    {
     "need": "a link inside a sentence",
     "id": "link"
    },
    {
     "need": "a divider with no label on it",
     "id": "separator"
    }
   ],
   "related": [
    "message",
    "badge",
    "separator"
   ],
   "exports": [
    "Marker",
    "MarkerIcon",
    "MarkerContent",
    "markerVariants"
   ],
   "aliases": [],
   "tokens": 781
  },
  {
   "id": "message",
   "kind": "component",
   "name": "Message",
   "import": "import { Message, MessageGroup, MessageAvatar, MessageContent, MessageHeader, MessageFooter } from \"@tecton/react/components/message\"",
   "family": "conversation",
   "summary": "One turn of a conversation: the avatar, the sender name, the message surface and the actions under it.",
   "fields": {
    "names": "message message message group message avatar message content message header message footer",
    "useWhen": "One turn of a conversation: the avatar, the sender name, the message surface and the actions under it. The turn belongs to a side — an assistant or tool row at the start, the user's own row at the end. Consecutive turns from the same sender should stack tightly: wrap them in `MessageGroup`.",
    "routed": "the whole turn, with avatar, sender name and message actions the row layout of one turn inside the transcript",
    "do": "Compose it: `MessageAvatar`, then `MessageContent` holding `MessageHeader`, a `Bubble` and `MessageFooter`. Choose the side with `align=\"start\" | \"end\"`; every part styles itself from the row's `data-align`. Put copy, retry and feedback controls in `MessageFooter` as `Button variant=\"ghost\"` with an `aria-label`. Wrap each `Message` in a `MessageScrollerItem` so the transcript can anchor, preserve and jump to it. Keep `className` for placement: `Message` owns the row direction, the gap and the text size."
   },
   "notFor": [
    {
     "need": "the framed surface the message text is printed on",
     "id": "bubble"
    },
    {
     "need": "a row of an application list, with media, title and actions",
     "id": "item"
    },
    {
     "need": "the scroll container that follows a growing transcript",
     "id": "message-scroller"
    },
    {
     "need": "a status or system line between turns",
     "id": "marker"
    }
   ],
   "related": [
    "bubble",
    "message-scroller",
    "marker"
   ],
   "exports": [
    "Message",
    "MessageGroup",
    "MessageAvatar",
    "MessageContent",
    "MessageHeader",
    "MessageFooter"
   ],
   "aliases": [],
   "tokens": 859
  },
  {
   "id": "message-scroller",
   "kind": "component",
   "name": "MessageScroller",
   "import": "import { MessageScrollerProvider, MessageScroller, MessageScrollerViewport, MessageScrollerContent, MessageScrollerItem, MessageScrollerButton, useMessageScroller, useMessageScrollerScrollable, useMessageScrollerVisibility } from \"@tecton/react/components/message-scroller\"",
   "family": "conversation",
   "summary": "A transcript grows while the reader is inside it: a reply streams in, or a group chat appends.",
   "fields": {
    "names": "message scroller message scroller provider message scroller message scroller viewport message scroller content message scroller item message scroller button use message scroller use message scroller scrollable use message scroller visibility",
    "useWhen": "A transcript grows while the reader is inside it: a reply streams in, or a group chat appends. Reopening a saved thread should land on the last turn, not on the absolute bottom edge, and a permalink, a search hit or a jump menu can drive it from outside the list.",
    "routed": "the scroll container that follows a growing transcript a chat transcript that follows new messages",
    "do": "Compose all of it: `MessageScrollerProvider`, `MessageScroller`, `MessageScrollerViewport`, `MessageScrollerContent`, `MessageScrollerItem`, `MessageScrollerButton`. Put the behaviour on the provider: `autoScroll` (off by default), `defaultScrollPosition=\"last-anchor\"`, `scrollPreviousItemPeek`. Give every row a stable `messageId`, `scrollAnchor` to the row that starts a turn, and the scroller its height from outside (`className=\"flex-1\"` in a constrained parent). Drive it from anywhere inside the provider with `useMessageScroller`, `useMessageScrollerScrollable` and `useMessageScrollerVisibility`."
   },
   "notFor": [
    {
     "need": "a styled scroll region for content that does not grow at its end",
     "id": "scroll-area"
    },
    {
     "need": "the row layout of one turn inside the transcript",
     "id": "message"
    }
   ],
   "related": [
    "message",
    "marker",
    "scroll-area"
   ],
   "exports": [
    "MessageScrollerProvider",
    "MessageScroller",
    "MessageScrollerViewport",
    "MessageScrollerContent",
    "MessageScrollerItem",
    "MessageScrollerButton",
    "useMessageScroller",
    "useMessageScrollerScrollable",
    "useMessageScrollerVisibility"
   ],
   "aliases": [],
   "tokens": 1009
  },
  {
   "id": "meter",
   "kind": "component",
   "name": "Meter",
   "import": "import { Meter } from \"@tecton/react/tecton/meter\"",
   "family": "progress",
   "summary": "A score or a level is being read, not a task being completed: geological risk, drilling complexity, confidence, data quality.",
   "fields": {
    "names": "meter meter",
    "useWhen": "A score or a level is being read, not a task being completed: geological risk, drilling complexity, confidence, data quality. The value sits on a fixed scale with named bands (low / medium / high). The readout belongs on a card, in a table cell or in a panel row, with a label and a word for the value.",
    "routed": "a value read against a fixed scale with named bands a segmented readout of risk, complexity or confidence a gauge of risk, complexity or confidence a value read against a fixed scale with named bands",
    "do": "Name it: `label` renders the visible label that names the meter; use `aria-label` when the name is already beside it. State the scale with `min` and `max` whenever it is not 0–100; the segments fill from the percentage. Choose `segments` (5 by default, `1` for a continuous bar) and `size=\"sm\" | \"md\" | \"lg\"`. Let `color=\"auto\"` pick success / warning / error from the value, or fix it with `color`; `color=\"custom\"` reads `--meter-fill`. Show the band with `valueLabel=\"Medium\"` and the raw number with `showValue`."
   },
   "notFor": [
    {
     "need": "the completion of a task that will finish",
     "id": "progress"
    },
    {
     "need": "an indeterminate wait",
     "id": "spinner"
    },
    {
     "need": "a one-word risk label in a row or a header",
     "id": "badge"
    }
   ],
   "related": [
    "progress",
    "circular-progress"
   ],
   "exports": [
    "Meter"
   ],
   "aliases": [],
   "tokens": 904
  },
  {
   "id": "native-select",
   "kind": "component",
   "name": "NativeSelect",
   "import": "import { NativeSelect, NativeSelectOption, NativeSelectOptGroup } from \"@tecton/react/components/native-select\"",
   "family": "selection",
   "summary": "You want the platform's own dropdown: native keyboard behaviour and the mobile wheel, with no popover.",
   "fields": {
    "names": "native select native select native select option native select opt group",
    "useWhen": "You want the platform's own dropdown: native keyboard behaviour and the mobile wheel, with no popover. A form posts natively and the value has to come from a real `select` element in the DOM. The row is dense and the options are plain text, with no icons or descriptions.",
    "routed": "a year or a month picked in a dense form row the browser's own dropdown, for a dense form or a phone",
    "do": "Treat it as a `select`: `value` / `defaultValue` / `onChange(event)`, `disabled`, `required`, `name`. Set the height with `size=\"sm\" | \"default\"`; the native row-count `size` is deliberately not available. Make the first `NativeSelectOption` an empty-valued row when you need a placeholder. Group options with `NativeSelectOptGroup label=\"…\"`. Signal errors with `aria-invalid` on the select and `data-invalid` on the surrounding `Field`."
   },
   "notFor": [
    {
     "need": "a themed popover with icons, rich items or a searchable list",
     "id": "select"
    },
    {
     "need": "typing to narrow a long list",
     "id": "combobox"
    },
    {
     "need": "two to five options that all stay visible",
     "id": "radio-group"
    }
   ],
   "related": [
    "select",
    "combobox"
   ],
   "exports": [
    "NativeSelect",
    "NativeSelectOption",
    "NativeSelectOptGroup"
   ],
   "aliases": [],
   "tokens": 782
  },
  {
   "id": "overflow",
   "kind": "component",
   "name": "Overflow",
   "import": "import { Overflow, Toolbar, OverflowItem, OverflowLabel, OverflowGroup, OverflowDivider, OverflowSpacer, OverflowMenu, useIsOverflowItemVisible } from \"@tecton/react/tecton/overflow\"",
   "family": "presentation",
   "summary": "A row of controls has to survive a container that gets narrow: a panel toolbar, a table header, a card's action row.",
   "fields": {
    "names": "overflow overflow toolbar overflow item overflow label overflow group overflow divider overflow spacer overflow menu use is overflow item visible",
    "useWhen": "A row of controls has to survive a container that gets narrow: a panel toolbar, a table header, a card's action row. The actions matter enough to stay reachable, so hiding them outright is not acceptable. The row is measured by its own width, not by a breakpoint — the same toolbar sits in a wide page and a 320 px aside.",
    "routed": "a toolbar that folds into a More menu by measured width",
    "do": "Wrap every control that may leave the row in an `OverflowItem` with a stable `value`; leave the primary action unwrapped so it is fixed and never collapses. Put the text in an `OverflowLabel` beside an icon and describe the menu entry with `label`, `icon`, `shortcut` and `onClick`; only an item with a `label` and an icon in its control goes icon-only, a text-only item keeps its label. Decide who leaves first with `priority` (higher stays longer); the menu always keeps source order. Use `Toolbar` with an `aria-label` when the row is a toolbar (arrow keys move between every visible control, Tab leaves it) and `Overflow` when it is not, such as a chip row or a header with a tab list. Give a non-button its menu form through `overflow`: a `DropdownMenuSub` for a select or submenu, a `DropdownMenuItem` opening a `Dialog` for a text input, or `\"never\"` to pin the item in the row."
   },
   "notFor": [
    {
     "need": "two or three buttons welded into one control",
     "id": "button-group"
    },
    {
     "need": "actions that appear only while rows are selected",
     "id": "action-bar"
    },
    {
     "need": "every action behind one button from the start",
     "id": "dropdown-menu"
    }
   ],
   "related": [
    "button-group",
    "action-bar",
    "dropdown-menu"
   ],
   "exports": [
    "Overflow",
    "Toolbar",
    "OverflowItem",
    "OverflowLabel",
    "OverflowGroup",
    "OverflowDivider",
    "OverflowSpacer",
    "OverflowMenu",
    "useIsOverflowItemVisible"
   ],
   "aliases": [],
   "tokens": 1240
  },
  {
   "id": "page-header",
   "kind": "component",
   "name": "PageHeader",
   "import": "import { PageHeader, PageHeaderContent, PageHeaderEyebrow, PageHeaderTitle, PageHeaderDescription, PageHeaderNav, PageHeaderActions } from \"@tecton/react/tecton/page-header\"",
   "family": "surfaces",
   "summary": "The block at the top of a page: what the user is looking at, and what they can do to it.",
   "fields": {
    "names": "page header page header page header content page header eyebrow page header title page header description page header nav page header actions",
    "useWhen": "The block at the top of a page: what the user is looking at, and what they can do to it. The title needs an eyebrow (a breadcrumb or category), a description, or section tabs. The actions have to survive a narrow window without the header stacking.",
    "routed": "the title block at the top of the page inside the main area actions that belong to the page rather than to the shell section tabs under a page title the title block at the top of a page the title block at the top of a page",
    "do": "Put the eyebrow, title and description inside `PageHeaderContent`; put the actions in `PageHeaderActions`. Leave the primary action unwrapped in `PageHeaderActions`, and wrap secondary ones in `OverflowItem` with a `priority` so they collapse first. Use `PageHeaderNav` for section tabs and give it an `aria-label`; it is a bare `nav`. Let `PageHeaderTitle` be the `h1`: one per page, no heading level or size set through `className`."
   },
   "notFor": [
    {
     "need": "the title row of a panel or an inspector",
     "id": "panel"
    },
    {
     "need": "the top bar of the application frame",
     "id": "app-shell"
    },
    {
     "need": "a heading inside a card",
     "id": "card"
    }
   ],
   "related": [
    "panel",
    "app-shell",
    "card"
   ],
   "exports": [
    "PageHeader",
    "PageHeaderContent",
    "PageHeaderEyebrow",
    "PageHeaderTitle",
    "PageHeaderDescription",
    "PageHeaderNav",
    "PageHeaderActions"
   ],
   "aliases": [],
   "tokens": 744
  },
  {
   "id": "pagination",
   "kind": "component",
   "name": "Pagination",
   "import": "import { Pagination, PaginationContent, PaginationItem, PaginationLink, PaginationPrevious, PaginationNext, PaginationEllipsis } from \"@tecton/react/components/pagination\"",
   "family": "navigation",
   "summary": "A list is served one page at a time and the user chooses which page.",
   "fields": {
    "names": "pagination pagination pagination content pagination item pagination link pagination previous pagination next pagination ellipsis",
    "useWhen": "A list is served one page at a time and the user chooses which page. The page has to be a URL: bookmarked, shared, opened in a new tab, restored on reload. Only \"previous\" and \"next\" are wanted, next to a rows-per-page `Select`.",
    "routed": "moving through the pages of one list",
    "do": "Compose `Pagination` > `PaginationContent` > one `PaginationItem` per control. Mark the page with `isActive` on `PaginationLink`: it swaps `ghost` for `outline` and sets `aria-current=\"page\"`. Give every control an `href`; `PaginationLink` is a `Button` rendered as an `a`, so a press is a navigation, not a state change. At the first or last page leave `PaginationPrevious` / `PaginationNext` out: a link has no disabled state. Elide the middle with `PaginationEllipsis`, and relabel the ends with `text` on `PaginationPrevious` and `PaginationNext`. Keep `className` to placement (`mx-0 w-auto`); the controls own their size, shape and colour."
   },
   "notFor": [
    {
     "need": "paging driven by a table's own sorting and filtering state",
     "id": "tanstack-table"
    },
    {
     "need": "switching between sibling sections of one page",
     "id": "tabs"
    },
    {
     "need": "the rows-per-page picker beside the controls",
     "id": "select"
    }
   ],
   "related": [
    "table",
    "tabs"
   ],
   "exports": [
    "Pagination",
    "PaginationContent",
    "PaginationItem",
    "PaginationLink",
    "PaginationPrevious",
    "PaginationNext",
    "PaginationEllipsis"
   ],
   "aliases": [],
   "tokens": 969
  },
  {
   "id": "panel",
   "kind": "component",
   "name": "Panel",
   "import": "import { Panel, PanelHeader, PanelTitle, PanelDescription, PanelActions, PanelContent, PanelFooter } from \"@tecton/react/tecton/panel\"",
   "family": "surfaces",
   "summary": "A tool surface inside the application: a properties panel, an inspector, a dashboard section.",
   "fields": {
    "names": "panel panel panel header panel title panel description panel actions panel content panel footer",
    "useWhen": "A tool surface inside the application: a properties panel, an inspector, a dashboard section. The panel is given a height by its container and its body has to scroll inside it. Actions belong to the panel itself — in the header row, or pinned at the bottom.",
    "routed": "a titled tool surface inside the work area a titled tool surface with a scrolling body and a pinned footer a titled tool surface whose body scrolls and whose footer stays put the trailing actions of a panel header the title row of a panel or an inspector a titled tool surface whose body scrolls the scrolling body of a titled tool surface the rule under a panel title a panel that belongs to the page instead of floating over it",
    "do": "Compose it: `PanelHeader` with `PanelTitle`, `PanelDescription` and `PanelActions`, then `PanelContent`, then `PanelFooter`. Choose the surface with `variant=\"default\" | \"elevated\" | \"flat\" | \"outline\"` and the padding with `size=\"sm\" | \"md\" | \"lg\"`. Put the body in `PanelContent`: it is the flex child that carries `min-h-0 flex-1 overflow-auto`. Give the panel its height from the outside (`className=\"h-72\"`, or an `AppShellAside`); the parts handle the rest. Keep one to three icon buttons in `PanelActions`; beyond that put an `Overflow` toolbar inside the slot and the title yields width to it."
   },
   "notFor": [
    {
     "need": "a self-contained content block that grows with its text",
     "id": "card"
    },
    {
     "need": "the title block at the top of a page",
     "id": "page-header"
    },
    {
     "need": "a panel that floats over the page and is dismissed",
     "id": "sheet"
    },
    {
     "need": "the application frame around the work area",
     "id": "app-shell"
    }
   ],
   "related": [
    "card",
    "app-shell",
    "sheet"
   ],
   "exports": [
    "Panel",
    "PanelHeader",
    "PanelTitle",
    "PanelDescription",
    "PanelActions",
    "PanelContent",
    "PanelFooter"
   ],
   "aliases": [],
   "tokens": 807
  },
  {
   "id": "popover",
   "kind": "component",
   "name": "Popover",
   "import": "import { Popover, PopoverTrigger, PopoverContent, PopoverHeader, PopoverTitle, PopoverDescription } from \"@tecton/react/components/popover\"",
   "family": "overlays",
   "summary": "A few controls belong to one trigger and should appear next to it: dimensions, a colour, a filter.",
   "fields": {
    "names": "popover popover popover trigger popover content popover header popover title popover description",
    "useWhen": "A few controls belong to one trigger and should appear next to it: dimensions, a colour, a filter. The user opens it deliberately by pressing, and the page behind it stays live. The content is interactive — inputs, buttons, a small form — so a `Tooltip` is out.",
    "routed": "a compact date field that opens a calendar on demand short content anchored to the control that opened it content the user opens deliberately and works in content anchored to the control that opened it content with a link, a button or a field inside it",
    "do": "Compose `Popover` (the root) > `PopoverTrigger render={<Button variant=\"outline\" />}` + `PopoverContent`; control it with `open` / `onOpenChange` on `Popover`. Position with `side` (`\"top\" | \"bottom\" | \"left\" | \"right\" | \"inline-start\" | \"inline-end\"`) and `align` (`\"start\" | \"center\" | \"end\"`) on `PopoverContent`, nudged with `sideOffset` / `alignOffset`. Structure the top with `PopoverHeader`, `PopoverTitle` and `PopoverDescription`; the content is already a `flex flex-col gap-4`. Use `className` on `PopoverContent` for width only (`w-80`): it owns its padding, radius, shadow and surface."
   },
   "notFor": [
    {
     "need": "a short label on a control",
     "id": "tooltip"
    },
    {
     "need": "a preview that appears when the pointer rests on a link",
     "id": "hover-card"
    },
    {
     "need": "a decision that must block the rest of the page",
     "id": "dialog"
    }
   ],
   "related": [
    "hover-card",
    "tooltip",
    "dialog"
   ],
   "exports": [
    "Popover",
    "PopoverTrigger",
    "PopoverContent",
    "PopoverHeader",
    "PopoverTitle",
    "PopoverDescription"
   ],
   "aliases": [],
   "tokens": 985
  },
  {
   "id": "progress",
   "kind": "component",
   "name": "Progress",
   "import": "import { Progress, ProgressLabel, ProgressValue, ProgressTrack, ProgressIndicator } from \"@tecton/react/components/progress\"",
   "family": "progress",
   "summary": "A task has a start, an end and a measurable fraction between them: an upload, an import, a batch run.",
   "fields": {
    "names": "progress progress progress label progress value progress track progress indicator",
    "useWhen": "A task has a start, an end and a measurable fraction between them: an upload, an import, a batch run. The bar belongs in the flow of a panel, a row or a dialog, across the available width. The number matters as much as the bar: `ProgressLabel` plus `ProgressValue`.",
    "routed": "a full-width determinate bar in the flow of a panel the completion of a task that will finish a measurable percentage the progress of a task the user is watching a measurable percentage",
    "do": "Name it: a `ProgressLabel` child, or `aria-label` when the label is already on screen. Let `ProgressValue` print the number; it is formatted from `value`, `min`, `max` and `format` (`Intl.NumberFormat` options). State the scale when it is not 0–100: `value={loaded} max={total}`. Pass `value={null}` while the total is unknown; the bar turns indeterminate. `className` sets the width (`w-full max-w-sm`), never the height or the colour."
   },
   "notFor": [
    {
     "need": "a gauge of risk, complexity or confidence",
     "id": "meter"
    },
    {
     "need": "a ring with the value in the middle",
     "id": "circular-progress"
    },
    {
     "need": "a placeholder for content whose progress cannot be measured",
     "id": "skeleton"
    },
    {
     "need": "a busy indicator inside a button",
     "id": "spinner"
    }
   ],
   "related": [
    "circular-progress",
    "meter",
    "spinner"
   ],
   "exports": [
    "Progress",
    "ProgressLabel",
    "ProgressValue",
    "ProgressTrack",
    "ProgressIndicator"
   ],
   "aliases": [],
   "tokens": 946
  },
  {
   "id": "provider",
   "kind": "component",
   "name": "TectonProvider",
   "import": "import { TectonProvider, useDirection, useLocale } from \"@tecton/react/tecton/provider\"",
   "family": "infrastructure",
   "summary": "Any Tecton application: mounted once at the root, it carries the locale, the reading direction and the client-side router every component reads.",
   "fields": {
    "names": "provider tecton provider use direction use locale",
    "useWhen": "Any Tecton application: mounted once at the root, it carries the locale, the reading direction and the client-side router every component reads. The application, or one subtree, reads right to left or in another language than the browser's. Overlays have to portal into a particular element instead of `document.body`.",
    "routed": "the locale, direction or router of an application that owns the document",
    "do": "Mount one `TectonProvider` above the router outlet and set the same `dir` and `lang` on `<html>`, so the document's own text flow and scrollbars match. Pass `locale` when you have one — it sets the direction too — and `direction` only to override it. Hand the router over once with `navigate` and `useHref`; Tecton links then navigate without a page load and still open in a new tab on ⌘/Ctrl-click. Nest a second provider for a subtree that differs: it inherits every value it does not set. Read the direction with `useDirection()` (`\"ltr\" | \"rtl\"`) in components of your own, never from `document.dir`."
   },
   "notFor": [
    {
     "need": "the root of an independently deployed application, overlay container included",
     "id": "theme-root"
    },
    {
     "need": "a dark or light island inside the application",
     "id": "theme-root"
    },
    {
     "need": "a link that navigates with the router",
     "id": "link"
    }
   ],
   "related": [
    "theme-root",
    "link"
   ],
   "exports": [
    "TectonProvider",
    "useDirection",
    "useLocale"
   ],
   "aliases": [],
   "tokens": 1009
  },
  {
   "id": "questionnaire",
   "kind": "component",
   "name": "Questionnaire",
   "import": "import { Questionnaire, QuestionnaireProgress, QuestionnaireItem, QuestionnaireTitle, QuestionnaireDescription, QuestionnaireChoices, QuestionnaireChoice, QuestionnaireChoiceDescription, QuestionnaireInput, QuestionnaireError, QuestionnaireActions, QuestionnairePrevious, QuestionnaireSkip, QuestionnaireNext, QuestionnaireSubmit } from \"@tecton/react/components/questionnaire\"",
   "family": "conversation",
   "summary": "The assistant asks a short, ordered set of questions before it acts: scope, constraints, review.",
   "fields": {
    "names": "questionnaire questionnaire questionnaire progress questionnaire item questionnaire title questionnaire description questionnaire choices questionnaire choice questionnaire choice description questionnaire input questionnaire error questionnaire actions questionnaire previous questionnaire skip questionnaire next questionnaire submit",
    "useWhen": "The assistant asks a short, ordered set of questions before it acts: scope, constraints, review. One question is on screen at a time, with progress, back, skip and submit around it, and the answers are gathered once, on submit.",
    "routed": "a structured question with choices for the user to answer",
    "do": "Declare the questions once as `items` on `Questionnaire`, so progress, actions and shortcuts render before hydration. Give every `QuestionnaireItem` a `name`, mark it `required` or `multiple`, and put its options in `QuestionnaireChoices`. Read the answers in `onSubmit` from `new FormData(event.currentTarget)`; the root is the `form`. Navigate only with `QuestionnairePrevious`, `QuestionnaireSkip`, `QuestionnaireNext` and `QuestionnaireSubmit`, inside `QuestionnaireActions`. These parts are plain form elements: they take `disabled` and `onChange`, and `QuestionnaireInput` needs an `aria-label`."
   },
   "notFor": [
    {
     "need": "one labelled control with a description and an error, inside a form",
     "id": "field"
    },
    {
     "need": "a single group of radio options that is part of a larger form",
     "id": "radio-group"
    }
   ],
   "related": [
    "field",
    "radio-group",
    "dialog"
   ],
   "exports": [
    "Questionnaire",
    "QuestionnaireProgress",
    "QuestionnaireItem",
    "QuestionnaireTitle",
    "QuestionnaireDescription",
    "QuestionnaireChoices",
    "QuestionnaireChoice",
    "QuestionnaireChoiceDescription",
    "QuestionnaireInput",
    "QuestionnaireError",
    "QuestionnaireActions",
    "QuestionnairePrevious",
    "QuestionnaireSkip",
    "QuestionnaireNext",
    "QuestionnaireSubmit"
   ],
   "aliases": [],
   "tokens": 1017
  },
  {
   "id": "radio-group",
   "kind": "component",
   "name": "RadioGroup",
   "import": "import { RadioGroup, RadioGroupItem } from \"@tecton/react/components/radio-group\"",
   "family": "selection",
   "summary": "Two to about six mutually exclusive options are all worth showing at once.",
   "fields": {
    "names": "radio group radio group radio group item",
    "useWhen": "Two to about six mutually exclusive options are all worth showing at once. Each option earns a description or a whole choice card, not just a word. The set is fixed and belongs in a form, next to the other fields.",
    "routed": "one choice out of several mutually exclusive options two to five options that all stay visible a single group of radio options that is part of a larger form two to five options that all stay visible",
    "do": "Drive it with `value` / `defaultValue` / `onValueChange` on `RadioGroup`; the handler receives the value, not an event. Give each `RadioGroupItem` a `value`; add `id` only so a `FieldLabel htmlFor` can point at it. Name the group with `aria-label`, or wrap it in `FieldSet` and `FieldLegend`. Disable everything with `disabled` on the group, one option with `disabled` on the item. Mark errors with `aria-invalid` on the group and `data-invalid` on each `Field` row."
   },
   "notFor": [
    {
     "need": "more than a handful of options",
     "id": "select"
    },
    {
     "need": "a compact segmented control in a toolbar",
     "id": "toggle-group"
    },
    {
     "need": "switching between panels of content",
     "id": "tabs"
    }
   ],
   "related": [
    "select",
    "toggle-group"
   ],
   "exports": [
    "RadioGroup",
    "RadioGroupItem"
   ],
   "aliases": [],
   "tokens": 991
  },
  {
   "id": "resizable",
   "kind": "component",
   "name": "ResizablePanelGroup",
   "import": "import { ResizablePanelGroup, ResizablePanel, ResizableHandle } from \"@tecton/react/components/resizable\"",
   "family": "layout",
   "summary": "Two or three regions share one axis and the user decides how the space is split: a tree beside an editor, a list above a detail.",
   "fields": {
    "names": "resizable resizable panel group resizable panel resizable handle",
    "useWhen": "Two or three regions share one axis and the user decides how the space is split: a tree beside an editor, a list above a detail. The split is a working preference worth remembering (`defaultLayout` in, `onLayoutChanged` out), not a fixed design. The regions are peers; neither is chrome wrapped around the other.",
    "routed": "",
    "do": "Size panels with unit strings on `defaultSize`, `minSize` and `maxSize` — `\"25%\"`, `\"320px\"`, `\"20rem\"` — and show a grip with `withHandle` when the divider should be obvious. Give the group a height from outside (`className=\"h-96\"`, or a flex parent): it is `flex h-full w-full` and brings none of its own. This module wraps react-resizable-panels: the props are that library's own (`disabled`, `collapsible`, `onLayoutChanged`, `panelRef`), and a `ResizableHandle` must be a direct child of its group."
   },
   "notFor": [
    {
     "need": "a draggable divider between the regions of the application frame",
     "id": "app-shell"
    },
    {
     "need": "a navigation rail the user collapses and expands",
     "id": "sidebar"
    },
    {
     "need": "a titled tool surface whose body scrolls",
     "id": "panel"
    }
   ],
   "related": [
    "app-shell",
    "panel",
    "scroll-area"
   ],
   "exports": [
    "ResizablePanelGroup",
    "ResizablePanel",
    "ResizableHandle"
   ],
   "aliases": [],
   "tokens": 1084
  },
  {
   "id": "scroll-area",
   "kind": "component",
   "name": "ScrollArea",
   "import": "import { ScrollArea, ScrollBar } from \"@tecton/react/components/scroll-area\"",
   "family": "layout",
   "summary": "A bounded region has more content than height: a tag list, a log, a long option list inside a card.",
   "fields": {
    "names": "scroll area scroll area scroll bar",
    "useWhen": "A bounded region has more content than height: a tag list, a log, a long option list inside a card. The scrollbar is going to be seen often enough that the browser default looks out of place. The region scrolls on its own while the page around it stays still.",
    "routed": "a row of cards the reader scrolls freely a styled scroll region for content that does not grow at its end",
    "do": "Give it a size: `h-*` or `max-h-*`, or `min-h-0 flex-1` when it is the growing child of a flex column; the viewport inside is `size-full` of it. Scroll sideways by making the content wider than the box (`whitespace-nowrap`, or an inner `flex w-max` row) and adding `<ScrollBar orientation=\"horizontal\" />` as a child. Put the padding on a wrapper inside the area, so the border and the scrollbar stay outside it. Leave focus to the component: the viewport becomes a tab stop by itself whenever it overflows."
   },
   "notFor": [
    {
     "need": "a chat transcript that follows new messages",
     "id": "message-scroller"
    },
    {
     "need": "the scrolling body of a titled tool surface",
     "id": "panel"
    },
    {
     "need": "the scrolling work area of the application frame",
     "id": "app-shell"
    }
   ],
   "related": [
    "panel",
    "message-scroller",
    "resizable"
   ],
   "exports": [
    "ScrollArea",
    "ScrollBar"
   ],
   "aliases": [],
   "tokens": 855
  },
  {
   "id": "select",
   "kind": "component",
   "name": "Select",
   "import": "import { Select, SelectTrigger, SelectValue, SelectContent, SelectGroup, SelectLabel, SelectItem, SelectSeparator, SelectScrollUpButton, SelectScrollDownButton, selectTriggerVariants } from \"@tecton/react/components/select\"",
   "family": "selection",
   "summary": "A field picks one value out of a known, closed list: a datum, a unit, a status.",
   "fields": {
    "names": "select select select trigger select value select content select group select label select item select separator select scroll up button select scroll down button select trigger variants",
    "useWhen": "A field picks one value out of a known, closed list: a datum, a unit, a status. The list is short enough to scan and needs no typing to get through. The trigger must match the other controls in a field row: `variant=\"outline\" | \"filled\" | \"text\"`.",
    "routed": "a short fixed list that needs no typing choosing a value for a form field a themed popover with icons, rich items or a searchable list the rows-per-page picker beside the controls more than a handful of options picking a value for a form field",
    "do": "Drive it with `value` / `defaultValue` / `onValueChange` on `Select`, identify each `SelectItem` by `value`, and narrow the `null` that `onValueChange` hands over when nothing is selected. Pass `items` (`{ msl: \"Mean sea level\" }` or `[{ value, label }]`) so `SelectValue` shows the label, not the raw value; its `placeholder` shows while nothing is selected. In a `Field`, put the `id` on `SelectTrigger` and point `FieldLabel htmlFor` at it. Choose the surface with `SelectTrigger`'s `variant`, the height with its `size=\"sm\" | \"default\"`; place the list with `side` / `align` on `SelectContent`. Structure long lists with `SelectGroup` and `SelectLabel`, divided by `SelectSeparator`."
   },
   "notFor": [
    {
     "need": "a list the user narrows by typing",
     "id": "combobox"
    },
    {
     "need": "the browser's own dropdown, for a dense form or a phone",
     "id": "native-select"
    },
    {
     "need": "two to five options that all stay visible",
     "id": "radio-group"
    },
    {
     "need": "a menu of actions rather than a value",
     "id": "dropdown-menu"
    }
   ],
   "related": [
    "combobox",
    "native-select",
    "radio-group"
   ],
   "exports": [
    "Select",
    "SelectTrigger",
    "SelectValue",
    "SelectContent",
    "SelectGroup",
    "SelectLabel",
    "SelectItem",
    "SelectSeparator",
    "SelectScrollUpButton",
    "SelectScrollDownButton",
    "selectTriggerVariants"
   ],
   "aliases": [],
   "tokens": 1214
  },
  {
   "id": "separator",
   "kind": "component",
   "name": "Separator",
   "import": "import { Separator } from \"@tecton/react/components/separator\"",
   "family": "surfaces",
   "summary": "Two groups of content need a visible break that no heading or spacing gives them.",
   "fields": {
    "names": "separator separator",
    "useWhen": "Two groups of content need a visible break that no heading or spacing gives them. A toolbar or a meta row needs a vertical rule between clusters of controls. A labelled divider: a `role=\"separator\"` flex row with a `Separator` on each side of the text.",
    "routed": "a divider with no label on it",
    "do": "Pick the weight with `emphasis=\"subtle\" | \"default\" | \"strong\"`; these are the three Tecton divider levels. Set the axis with `orientation=\"vertical\"`, and let the parent flex row give it height — it carries `self-stretch`. Keep `className` to layout (`flex-1`, `my-2`): the variant owns colour and thickness. Reach for a component's own divider first — `ItemSeparator` in an `ItemGroup`, `border-b` on a `CardHeader`, `PanelHeader`'s built-in rule."
   },
   "notFor": [
    {
     "need": "a divider between the rows of a list",
     "id": "item"
    },
    {
     "need": "the rule under a panel title",
     "id": "panel"
    },
    {
     "need": "the borders between the regions of the application frame",
     "id": "app-shell"
    }
   ],
   "related": [
    "item",
    "panel",
    "card"
   ],
   "exports": [
    "Separator"
   ],
   "aliases": [],
   "tokens": 735
  },
  {
   "id": "sheet",
   "kind": "component",
   "name": "Sheet",
   "import": "import { Sheet, SheetTrigger, SheetContent, SheetClose, SheetHeader, SheetTitle, SheetDescription, SheetFooter } from \"@tecton/react/components/sheet\"",
   "family": "overlays",
   "summary": "Details, filters or settings that belong to what is on screen: the page stays behind the overlay.",
   "fields": {
    "names": "sheet sheet sheet trigger sheet content sheet close sheet header sheet title sheet description sheet footer",
    "useWhen": "Details, filters or settings that belong to what is on screen: the page stays behind the overlay. The content is taller than it is wide — a form, a property list, a history. The user should be able to leave by clicking the page behind it.",
    "routed": "a panel that floats over the page and is dismissed a panel that slides in over the page and is dismissed a side panel that keeps the page visible behind it a side panel on a pointer-driven screen a panel that floats over the page and is dismissed a panel that floats over the content and is dismissed",
    "do": "Compose `Sheet` (the root) > `SheetTrigger render={<Button />}` + `SheetContent`, exactly as for `Dialog`; state is `open` / `onOpenChange` on `Sheet`. Pick the edge with `side=\"right\" | \"left\" | \"top\" | \"bottom\"` on `SheetContent`. Let `SheetHeader` and `SheetFooter` carry their own `p-4`; the sheet body is yours, so give it `px-4` and `flex-1`. Drop the corner control with `showCloseButton={false}` on `SheetContent` and close from the footer with `SheetClose render={<Button variant=\"outline\" />}`. Set width with `className` on `SheetContent` (`sm:max-w-lg`) only; it owns the surface, the edge and the animation."
   },
   "notFor": [
    {
     "need": "a decision that must be finished before anything else",
     "id": "dialog"
    },
    {
     "need": "a bottom sheet the user drags with a thumb on touch",
     "id": "drawer"
    },
    {
     "need": "a panel that belongs to the page instead of floating over it",
     "id": "panel"
    },
    {
     "need": "content anchored to the control that opened it",
     "id": "popover"
    }
   ],
   "related": [
    "dialog",
    "drawer",
    "panel"
   ],
   "exports": [
    "Sheet",
    "SheetTrigger",
    "SheetContent",
    "SheetClose",
    "SheetHeader",
    "SheetTitle",
    "SheetDescription",
    "SheetFooter"
   ],
   "aliases": [],
   "tokens": 948
  },
  {
   "id": "sidebar",
   "kind": "component",
   "name": "Sidebar",
   "import": "import { Sidebar, SidebarProvider, SidebarTrigger, SidebarRail, SidebarInset, SidebarHeader, SidebarContent, SidebarFooter, SidebarGroup, SidebarGroupLabel, SidebarMenu, SidebarMenuItem, SidebarMenuButton, SidebarMenuAction, SidebarMenuBadge, SidebarMenuSub, SidebarMenuSubItem, SidebarMenuSubButton, useSidebar } from \"@tecton/react/components/sidebar\"",
   "family": "navigation",
   "summary": "The application's navigation rail: groups of destinations, an active row, a collapse control.",
   "fields": {
    "names": "sidebar sidebar sidebar provider sidebar trigger sidebar rail sidebar inset sidebar header sidebar content sidebar footer sidebar group sidebar group label sidebar menu sidebar menu item sidebar menu button sidebar menu action sidebar menu badge sidebar menu sub sidebar menu sub item sidebar menu sub button use sidebar",
    "useWhen": "The application's navigation rail: groups of destinations, an active row, a collapse control. The rail has to survive a phone — below the mobile breakpoint `Sidebar` renders itself as a `Sheet`. Rows carry a count, a per-row action or a second level of destinations.",
    "routed": "a navigation rail the user collapses and expands",
    "do": "Wrap the rail, the content and every trigger in one `SidebarProvider`; `Sidebar`, `SidebarTrigger`, `SidebarRail` and `SidebarMenuButton` all call `useSidebar`, which throws outside it. Render a row as `SidebarMenuItem` > `SidebarMenuButton`, with `render={<a href=\"/wells\" />}` (or the router's `Link`) for a destination and `isActive` for the current one. Pick the behaviour with `collapsible=\"offcanvas\" | \"icon\" | \"none\"` and the surface with `variant=\"sidebar\" | \"floating\" | \"inset\"`. Pass `tooltip=\"Wells\"` (or `TooltipContent` props) on `SidebarMenuButton`: it shows only while the rail is icon-collapsed, which is exactly when the label is gone."
   },
   "notFor": [
    {
     "need": "a fixed navigation column inside the application frame",
     "id": "app-shell"
    },
    {
     "need": "a panel that floats over the content and is dismissed",
     "id": "sheet"
    },
    {
     "need": "a hierarchy of folders and records expanded in place",
     "id": "tree-view"
    }
   ],
   "related": [
    "app-shell",
    "sheet",
    "tree-view"
   ],
   "exports": [
    "Sidebar",
    "SidebarProvider",
    "SidebarTrigger",
    "SidebarRail",
    "SidebarInset",
    "SidebarHeader",
    "SidebarContent",
    "SidebarFooter",
    "SidebarGroup",
    "SidebarGroupLabel",
    "SidebarMenu",
    "SidebarMenuItem",
    "SidebarMenuButton",
    "SidebarMenuAction",
    "SidebarMenuBadge",
    "SidebarMenuSub",
    "SidebarMenuSubItem",
    "SidebarMenuSubButton",
    "useSidebar"
   ],
   "aliases": [],
   "tokens": 1138
  },
  {
   "id": "skeleton",
   "kind": "component",
   "name": "Skeleton",
   "import": "import { Skeleton } from \"@tecton/react/components/skeleton\"",
   "family": "progress",
   "summary": "The shape of the content is known before the data arrives: a card, a table, a form, a list of rows.",
   "fields": {
    "names": "skeleton skeleton",
    "useWhen": "The shape of the content is known before the data arrives: a card, a table, a form, a list of rows. The wait is long enough to notice, and the layout must not move when the data lands. Several regions load independently and each should keep its place.",
    "routed": "a placeholder shape while the content loads a placeholder shape while real content loads a placeholder while the content is still loading a placeholder for content whose progress cannot be measured a placeholder that holds the page layout while content loads",
    "do": "Give every `Skeleton` a size from the spacing scale: `h-4 w-full`, `size-12 rounded-full`, `aspect-video w-full`. Mirror the real layout — the same wrappers, the same gaps, one `Skeleton` per box the content will occupy. Vary the widths with fractions (`w-2/3`, `w-3/4`) so a block reads as text. `className` is for the box; the pulse and the muted surface belong to the component."
   },
   "notFor": [
    {
     "need": "a measurable percentage",
     "id": "progress"
    },
    {
     "need": "a busy indicator inside a button or an input",
     "id": "spinner"
    },
    {
     "need": "the state when there is nothing to load",
     "id": "empty"
    }
   ],
   "related": [
    "spinner",
    "empty",
    "progress"
   ],
   "exports": [
    "Skeleton"
   ],
   "aliases": [],
   "tokens": 718
  },
  {
   "id": "slider",
   "kind": "component",
   "name": "Slider",
   "import": "import { Slider } from \"@tecton/react/components/slider\"",
   "family": "forms",
   "summary": "An approximate value inside a known range: opacity, threshold, zoom, temperature.",
   "fields": {
    "names": "slider slider",
    "useWhen": "An approximate value inside a known range: opacity, threshold, zoom, temperature. A range with two or more thumbs, such as a price or depth filter. The exact number matters less than the gesture of moving through the range.",
    "routed": "a value picked from a continuous range",
    "do": "Bound the range with `min`, `max` and `step` (0–100 by default). Pass `value` / `defaultValue` as an array — one entry per thumb — and read it back in `onValueChange`, whose type is `number | number[]`, so narrow it; `onValueCommitted` fires once on release. Name it with `aria-labelledby` pointing at the visible title's `id`: the slider forwards it to every thumb's range input, which `aria-label` on the root does not reach. Inside a `Field`, use `FieldTitle` (with that `id`) and `FieldDescription`, and show the live value in the description. Disable with `disabled`; switch axis with `orientation=\"vertical\"` plus a height class such as `h-40`."
   },
   "notFor": [
    {
     "need": "an exact number the user types, with units or validation",
     "id": "input"
    },
    {
     "need": "the title, description and layout around the control",
     "id": "field"
    },
    {
     "need": "an on/off setting rather than a value",
     "id": "switch"
    }
   ],
   "related": [
    "field",
    "input"
   ],
   "exports": [
    "Slider"
   ],
   "aliases": [],
   "tokens": 814
  },
  {
   "id": "sonner",
   "kind": "component",
   "name": "Toaster + toast",
   "import": "import { Toaster } from \"@tecton/react/components/sonner\"\nimport { toast } from \"sonner\"",
   "family": "feedback",
   "summary": "Confirming something the user just did, where the page already shows the result: saved, copied, queued, undone.",
   "fields": {
    "names": "sonner toaster toast",
    "useWhen": "Confirming something the user just did, where the page already shows the result: saved, copied, queued, undone. Reporting the end of a background task the user is not watching: an export finished, a run failed to start. Offering a short-lived undo beside that confirmation.",
    "routed": " a transient confirmation that carries no actions a confirmation of something the user can undo afterwards a confirmation that should disappear on its own a confirmation that disappears on its own a confirmation that a tag was removed a confirmation that appears away from the control a confirmation that disappears on its own",
    "do": "Mount `<Toaster />` once, at the application root; everywhere else import `toast` from `sonner` and call it. Use the typed helpers — `toast.success`, `toast.info`, `toast.warning`, `toast.error`, `toast.promise` — so the Toaster's icons and the Tecton outlined status colours apply. Put the detail in `description` and a single undo in `action`; a toast is one sentence and at most one action. Set the placement once on the Toaster (`position=\"top-center\"`), not per call, and let `toast.promise` own a pending toast so it resolves itself."
   },
   "notFor": [
    {
     "need": "a message that must stay until the user resolves it",
     "id": "alert"
    },
    {
     "need": "the state of a list that has nothing in it",
     "id": "empty"
    },
    {
     "need": "the progress of a task the user is watching",
     "id": "progress"
    }
   ],
   "related": [
    "alert",
    "empty"
   ],
   "exports": [
    "Toaster"
   ],
   "aliases": [
    "toast"
   ],
   "tokens": 898
  },
  {
   "id": "spinner",
   "kind": "component",
   "name": "Spinner",
   "import": "import { Spinner } from \"@tecton/react/components/spinner\"",
   "family": "progress",
   "summary": "A control is busy: a `Button` that is saving, an `InputGroup` that is validating, a `Badge` that says \"Generating\".",
   "fields": {
    "names": "spinner spinner",
    "useWhen": "A control is busy: a `Button` that is saving, an `InputGroup` that is validating, a `Badge` that says \"Generating\". A small region is waiting, and the wait is short and cannot be measured. An `Empty` state stands in for a running operation, with the spinner inside `EmptyMedia`.",
    "routed": "a small busy glyph inside a button or an input a busy indicator on an icon button an indeterminate wait a busy indicator inside a button a busy indicator inside a button or an input",
    "do": "Inside a `Button`, a `Badge` or an `InputGroupAddon`, give it `data-icon=\"inline-start\"` or `data-icon=\"inline-end\"` so the control trims its padding on that side. Mark the button that is working `disabled` with `focusableWhenDisabled`, so it keeps focus and cannot be pressed twice; make the label a verb: \"Saving…\". Resize with `size-*` only (`size-3`, `size-6`); the stroke is `currentColor`, so set the colour on the parent with a semantic token. `Spinner` already carries `role=\"status\"` and `aria-label=\"Loading\"`; do not wrap it in a second live region."
   },
   "notFor": [
    {
     "need": "a measurable percentage",
     "id": "progress"
    },
    {
     "need": "a ring with the value in the middle",
     "id": "circular-progress"
    },
    {
     "need": "a placeholder that holds the page layout while content loads",
     "id": "skeleton"
    }
   ],
   "related": [
    "skeleton",
    "circular-progress",
    "progress"
   ],
   "exports": [
    "Spinner"
   ],
   "aliases": [],
   "tokens": 859
  },
  {
   "id": "stat",
   "kind": "component",
   "name": "Stat",
   "import": "import { Stat, StatLabel, StatValue, StatDelta, StatHelp, StatGroup } from \"@tecton/react/tecton/stat\"",
   "family": "data",
   "summary": "A card, a panel or a header reports one measured number: cost per barrel, NPV, drilling days.",
   "fields": {
    "names": "stat stat stat label stat value stat delta stat help stat group",
    "useWhen": "A card, a panel or a header reports one measured number: cost per barrel, NPV, drilling days. The number wants a unit and a change against a baseline or a previous period. Several KPIs sit side by side and their digits have to align: that is `StatGroup`.",
    "routed": "one measured number with a unit and a change",
    "do": "Compose the parts inside `Stat`: `StatLabel`, `StatValue` with its `unit`, `StatDelta`, `StatHelp`. Scale the value with `size=\"sm\" | \"md\" | \"lg\"` (14 / 20 / 28 px) and place the block with `align=\"start\" | \"center\" | \"end\"`. Say which way the number moved with `StatDelta`'s `trend=\"up\" | \"down\" | \"flat\"` (the icon), and whether that is good news with `tone=\"positive\" | \"negative\" | \"neutral\"` (the colour). `tone` follows the trend unless you set it: set `tone=\"positive\"` on a falling cost, downtime or risk. Pass the unit to `StatValue`'s `unit` prop rather than writing it into the text, so it renders in the sans face at `0.6em`. Lay a row of KPIs out with `StatGroup`, the responsive `auto-fit` grid; `className` on it is for width and placement only."
   },
   "notFor": [
    {
     "need": "a word that names a status or a category",
     "id": "badge"
    },
    {
     "need": "a value read against a fixed scale with named bands",
     "id": "meter"
    },
    {
     "need": "a series compared over time or across categories",
     "id": "chart"
    }
   ],
   "related": [
    "meter",
    "badge",
    "chart"
   ],
   "exports": [
    "Stat",
    "StatLabel",
    "StatValue",
    "StatDelta",
    "StatHelp",
    "StatGroup"
   ],
   "aliases": [],
   "tokens": 990
  },
  {
   "id": "switch",
   "kind": "component",
   "name": "Switch",
   "import": "import { Switch } from \"@tecton/react/components/switch\"",
   "family": "forms",
   "summary": "A setting that takes effect the moment it is flipped, with no submit step.",
   "fields": {
    "names": "switch switch",
    "useWhen": "A setting that takes effect the moment it is flipped, with no submit step. An on/off preference in a settings panel or a choice card. The two states are opposites of one thing, not two options to choose between.",
    "routed": "a setting that takes effect the moment it is flipped an on/off setting rather than a value",
    "do": "Control it with `checked` and `onCheckedChange(checked: boolean)`, or leave it uncontrolled with `defaultChecked`. Pick the scale with `size=\"sm\" | \"default\"`; the thumb travel is matched to each. Give it an `id` and point a `FieldLabel htmlFor` at it inside a `Field orientation=\"horizontal\"`, with `FieldContent` for the description. Disable with `disabled` and put `data-disabled` on the `Field`; for an invalid value set `aria-invalid` on the `Switch` and `data-invalid` on the `Field`. For a choice card, wrap the whole `Field` in a `FieldLabel` so the card surface is the hit target."
   },
   "notFor": [
    {
     "need": "a term the user ticks as part of submitting a form",
     "id": "checkbox"
    },
    {
     "need": "the label, description and layout around the control",
     "id": "field"
    },
    {
     "need": "a value picked from a continuous range",
     "id": "slider"
    }
   ],
   "related": [
    "checkbox",
    "field"
   ],
   "exports": [
    "Switch"
   ],
   "aliases": [],
   "tokens": 677
  },
  {
   "id": "table",
   "kind": "component",
   "name": "Table",
   "import": "import { Table, TableHeader, TableBody, TableFooter, TableHead, TableRow, TableCell, TableCaption } from \"@tecton/react/components/table\"",
   "family": "data",
   "summary": "Records of the same shape have to line up in columns: invoices, wells, run results.",
   "fields": {
    "names": "table table table header table body table footer table head table row table cell table caption",
    "useWhen": "Records of the same shape have to line up in columns: invoices, wells, run results. A footer totals the columns, or a caption names the table. The rows are read, compared or opened; interaction beyond that (sorting, paging) comes from TanStack Table on these parts.",
    "routed": "",
    "do": "Compose plain HTML: `Table` > `TableHeader` > `TableRow` > `TableHead`, `TableBody` > `TableRow` > `TableCell`, optional `TableFooter` and `TableCaption`. Select rows with a `Checkbox` per row (and one in the header for all) driven by your own state, and mark a selected row with `data-state=\"selected\"` for the `bg-table-active` fill. Open a record from a link in its identifying cell, not from a handler on the row. Show an empty result as `Empty` in place of the table, or inside one `TableCell colSpan={n}` when the header must stay; keep `className` on the parts to alignment and width (`text-right`, `w-24`)."
   },
   "notFor": [
    {
     "need": "sorting, filtering, paging or column visibility over the rows",
     "id": "tanstack-table"
    },
    {
     "need": "a list of rows with media, a title and actions rather than columns",
     "id": "item"
    }
   ],
   "related": [
    "item",
    "tree-view",
    "tanstack-table"
   ],
   "exports": [
    "Table",
    "TableHeader",
    "TableBody",
    "TableFooter",
    "TableHead",
    "TableRow",
    "TableCell",
    "TableCaption"
   ],
   "aliases": [],
   "tokens": 1091
  },
  {
   "id": "tabs",
   "kind": "component",
   "name": "Tabs",
   "import": "import { Tabs, TabsList, TabsTrigger, TabsContent, tabsListVariants } from \"@tecton/react/components/tabs\"",
   "family": "selection",
   "summary": "A page or panel has two to about six sections the user moves between without navigating away.",
   "fields": {
    "names": "tabs tabs tabs list tabs trigger tabs content tabs list variants",
    "useWhen": "A page or panel has two to about six sections the user moves between without navigating away. Exactly one section is visible at a time and each one is a real chunk of content. The strip belongs to the surface it sits on: a page header, a panel, a card.",
    "routed": "sections that replace each other in the same space switching between sibling views of the same page sections of content the user moves between sections the reader chooses by name switching between sibling sections of one page switching between panels of content switching between panels of content",
    "do": "Match every `TabsTrigger` to its `TabsContent` by `value`, the same string on both. Select with `defaultValue` / `value` and `onValueChange`, which receives the tab's value. Choose the strip with `TabsList` `variant=\"default\" | \"line\"`, and a side strip with `orientation=\"vertical\"` on `Tabs`. Give `TabsList` an `aria-label` when no nearby heading names the set; disable one tab with `disabled`."
   },
   "notFor": [
    {
     "need": "a filter that narrows a list in place",
     "id": "toggle-group"
    },
    {
     "need": "picking a value for a form field",
     "id": "select"
    }
   ],
   "related": [
    "toggle-group",
    "select"
   ],
   "exports": [
    "Tabs",
    "TabsList",
    "TabsTrigger",
    "TabsContent",
    "tabsListVariants"
   ],
   "aliases": [],
   "tokens": 939
  },
  {
   "id": "textarea",
   "kind": "component",
   "name": "Textarea",
   "import": "import { Textarea } from \"@tecton/react/components/textarea\"",
   "family": "forms",
   "summary": "Several lines of free text: a description, a note, a comment, a message.",
   "fields": {
    "names": "textarea textarea",
    "useWhen": "Several lines of free text: a description, a note, a comment, a message. The content grows while the user types and the box should follow it. Inside a `Field`, so the control arrives with a label and room for an error.",
    "routed": "a form field for a long description more than one line of free text",
    "do": "Pick the surface with `variant=\"outline\" | \"filled\" | \"text\"` — the same three axes as `Input`. Set the starting height with `rows`; the box already carries `field-sizing-content` and `min-h-16` and grows from there. `Textarea` is a real `<textarea>`: use `value`, `onChange(event)`, `disabled`, `required` and `aria-invalid`. Mirror the state on the `Field`: `data-disabled` beside `disabled`, `data-invalid` beside `aria-invalid`. Keep `className` to layout only — `w-full`, `col-span-2`."
   },
   "notFor": [
    {
     "need": "a single line of text",
     "id": "input"
    },
    {
     "need": "a toolbar, a counter or a send button attached to the box",
     "id": "input-group"
    },
    {
     "need": "the label, description and error message around the control",
     "id": "field"
    }
   ],
   "related": [
    "field",
    "input",
    "input-group"
   ],
   "exports": [
    "Textarea"
   ],
   "aliases": [],
   "tokens": 836
  },
  {
   "id": "theme-root",
   "kind": "component",
   "name": "ThemeRoot",
   "import": "import { ThemeRoot, themeRootVariants } from \"@tecton/react/tecton/theme-root\"",
   "family": "infrastructure",
   "summary": "An application is mounted into a page that already carries its own copy of Tecton: a Module Federation remote, an embedded widget.",
   "fields": {
    "names": "theme root theme root theme root variants",
    "useWhen": "An application is mounted into a page that already carries its own copy of Tecton: a Module Federation remote, an embedded widget. A subtree must be pinned against the shell's mode — a dark map or log viewer in a light application. That subtree's overlays have to stay inside its own `@scope` rule instead of landing in a bare `document.body`.",
    "routed": "the root of an independently deployed application, overlay container included a dark or light island inside the application",
    "do": "Render it around the whole mounted tree with the deployable's scope class, and keep layout on an inner element: `<ThemeRoot className=\"mfe-a\"><div className=\"flex h-full flex-col\">…</div></ThemeRoot>`. The overlay container that mirrors `className` is `display: contents`, so layout utilities do nothing there while typography and colour classes reach every overlay. Keep `theme=\"inherit\"` inside a Tecton shell — the shell's variables, palette and mode inherit — and pin `dark` or `light` only for an inverted island. Retint one root with an arbitrary property in `className`: `[--primary:var(--tecton-palette-green-560)]`, which is mirrored onto the overlay container; pass `overlayClassName` instead when only some of the root's classes belong on the overlays (the theme class is always added to it). Pass `dir` and `locale` when the remote reads differently from its shell: `dir` lands on the root and on the overlay container, and both feed the `TectonProvider` it renders. Pass `overlayContainer` to reuse an element the shell owns, or `null` to opt out; omit it and the root creates, syncs and removes its own."
   },
   "notFor": [
    {
     "need": "the locale, direction or router of an application that owns the document",
     "id": "provider"
    },
    {
     "need": "the header, rail and work area of an application that owns the document",
     "id": "app-shell"
    }
   ],
   "related": [
    "provider",
    "app-shell"
   ],
   "exports": [
    "ThemeRoot",
    "themeRootVariants"
   ],
   "aliases": [],
   "tokens": 1181
  },
  {
   "id": "toggle",
   "kind": "component",
   "name": "Toggle",
   "import": "import { Toggle, toggleVariants } from \"@tecton/react/components/toggle\"",
   "family": "actions",
   "summary": "One independent formatting or view option is on or off: bold, bookmark, show labels.",
   "fields": {
    "names": "toggle toggle toggle variants",
    "useWhen": "One independent formatting or view option is on or off: bold, bookmark, show labels. The control lives in a toolbar or beside content, not in a labelled form row. Pressing it changes the surrounding view immediately, with no save step.",
    "routed": "a single independent on/off control",
    "do": "Hold state with `pressed` / `defaultPressed` and read changes from `onPressedChange`, which receives a boolean. Pick `variant=\"default\" | \"outline\"` and `size=\"default\" | \"sm\" | \"lg\"`; they own colour, height and padding. Give an icon-only `Toggle` an `aria-label`, and mark a paired icon with `data-icon=\"inline-start\"`. Disable with `disabled`. Style the pressed look through the variant's `aria-pressed` rules, never with your own colour classes."
   },
   "notFor": [
    {
     "need": "several related toggles in one segmented control",
     "id": "toggle-group"
    },
    {
     "need": "a control that performs an action rather than holding state",
     "id": "button"
    }
   ],
   "related": [
    "toggle-group",
    "button"
   ],
   "exports": [
    "Toggle",
    "toggleVariants"
   ],
   "aliases": [],
   "tokens": 834
  },
  {
   "id": "toggle-group",
   "kind": "component",
   "name": "ToggleGroup",
   "import": "import { ToggleGroup, ToggleGroupItem } from \"@tecton/react/components/toggle-group\"",
   "family": "selection",
   "summary": "A small fixed set of options works as a filter or a formatting control in a toolbar.",
   "fields": {
    "names": "toggle group toggle group toggle group item",
    "useWhen": "A small fixed set of options works as a filter or a formatting control in a toolbar. Several of those options may be on at the same time (`multiple`). The options are icons or one-word labels that fit in a segmented row.",
    "routed": "options that hold an on or off state a compact segmented control in a toolbar a filter that narrows a list in place several related toggles in one segmented control",
    "do": "Identify each item by `value`; read `value` / `defaultValue` and `onValueChange`, which always hand you an **array** of values. Leave it single-select for an exclusive choice, add `multiple` for independent toggles. Shape the bar with `variant=\"outline\"`, `size` and `spacing={0}`; the joined corners and shared borders come from the variant. Give every icon-only `ToggleGroupItem` an `aria-label`, and stack with `orientation=\"vertical\"`."
   },
   "notFor": [
    {
     "need": "switching between panels of content",
     "id": "tabs"
    },
    {
     "need": "a single independent on/off control",
     "id": "toggle"
    },
    {
     "need": "buttons that perform actions rather than hold state",
     "id": "button-group"
    }
   ],
   "related": [
    "toggle",
    "tabs",
    "button-group"
   ],
   "exports": [
    "ToggleGroup",
    "ToggleGroupItem"
   ],
   "aliases": [],
   "tokens": 960
  },
  {
   "id": "tooltip",
   "kind": "component",
   "name": "Tooltip",
   "import": "import { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider } from \"@tecton/react/components/tooltip\"",
   "family": "overlays",
   "summary": "A control needs a few words of explanation: what an icon button does, why one is disabled.",
   "fields": {
    "names": "tooltip tooltip tooltip trigger tooltip content tooltip provider",
    "useWhen": "A control needs a few words of explanation: what an icon button does, why one is disabled. The text is a description, never the only place a fact appears.",
    "routed": "a short label on a control a short label on a control",
    "do": "Compose `Tooltip` (the root) > `TooltipTrigger render={<Button />}` + `TooltipContent`; position with `side` / `align` / `sideOffset` on `TooltipContent`. Wrap the app or the toolbar once in `TooltipProvider`: it sets the open delay (Tecton's is `0`) and lets adjacent tooltips open instantly; `delay` / `closeDelay` on one `TooltipTrigger` override it. Give an icon-only trigger its own `aria-label`; the tooltip describes, it does not name. To explain a disabled control, pass `focusableWhenDisabled` to the `Button` so hover and focus still reach the trigger; a `Kbd` inside gets its own spacing (`Save changes <Kbd>S</Kbd>`)."
   },
   "notFor": [
    {
     "need": "content with a link, a button or a field inside it",
     "id": "popover"
    },
    {
     "need": "a preview of what is behind a link",
     "id": "hover-card"
    },
    {
     "need": "a confirmation the user has to answer",
     "id": "alert-dialog"
    }
   ],
   "related": [
    "hover-card",
    "popover"
   ],
   "exports": [
    "Tooltip",
    "TooltipTrigger",
    "TooltipContent",
    "TooltipProvider"
   ],
   "aliases": [],
   "tokens": 1018
  },
  {
   "id": "tree-view",
   "kind": "component",
   "name": "TreeView",
   "import": "import { TreeView, TreeViewItem, TreeViewItemContent, TreeViewAction, TreeViewVisibilityToggle, TreeViewCollection } from \"@tecton/react/tecton/tree-view\"",
   "family": "data",
   "summary": "A hierarchy is browsed rather than read: a project tree, a field and well inventory, a layer list.",
   "fields": {
    "names": "tree view tree view tree view item tree view item content tree view action tree view visibility toggle tree view collection",
    "useWhen": "A hierarchy is browsed rather than read: a project tree, a field and well inventory, a layer list. Rows carry more than a label: selection, disabled, hidden, a colour, a count, a per-row menu. Arrow-key navigation, expansion and type-ahead have to work across the whole structure.",
    "routed": "an expandable hierarchy of folders and records a hierarchy of folders and records expanded in place",
    "do": "Give `TreeView` an `aria-label` and every `TreeViewItem` a unique `value`; add `textValue` when the row label is not plain text. Put `TreeViewItemContent` first inside each `TreeViewItem`, then the child `TreeViewItem`s — or `items` on `TreeView` and a `TreeViewCollection` per node for data. Drive state with string arrays: `selectionMode`, `value` / `defaultValue` / `onValueChange` and `expanded` / `defaultExpanded` / `onExpandedChange`; mark rows with `disabled` and `hidden` on `TreeViewItem`. Hang the extras on `TreeViewItemContent`: `colorTag`, `suffix`, `endAdornment` (`TreeViewVisibilityToggle` with `visible` / `onVisibleChange`, `TreeViewAction` with `onClick`). Style rows from the presence attributes `data-selected`, `data-expanded`, `data-disabled`, `data-hidden` and from `:hover` / `:focus-visible` (`group-hover/tree-item:opacity-100`). Write the selected state as `data-[selected]:…`: shadcn's `data-selected:` variant matches only `\"true\"`."
   },
   "notFor": [
    {
     "need": "stacked sections that expand, with no parent and child structure",
     "id": "accordion"
    },
    {
     "need": "a flat list of rows with media, a title and actions",
     "id": "item"
    }
   ],
   "related": [
    "accordion",
    "item",
    "color-swatch"
   ],
   "exports": [
    "TreeView",
    "TreeViewItem",
    "TreeViewItemContent",
    "TreeViewAction",
    "TreeViewVisibilityToggle",
    "TreeViewCollection"
   ],
   "aliases": [],
   "tokens": 1060
  },
  {
   "id": "tanstack-table",
   "kind": "external",
   "name": "TanStack Table",
   "import": "import { useReactTable } from \"@tanstack/react-table\"",
   "summary": "Data tables are a recipe on the shadcn Table, not a component.",
   "fields": {
    "names": "tanstack table tan stack table ",
    "useWhen": "Data tables are a recipe on the shadcn Table, not a component.",
    "routed": "rows with sorting, paging and selection rows with columns, sorting, paging and selection paging driven by a table's own sorting and filtering state sorting, filtering, paging or column visibility over the rows",
    "do": ""
   },
   "notFor": [],
   "related": [],
   "exports": [],
   "aliases": [
    "react-table"
   ],
   "tokens": 49
  },
  {
   "id": "tanstack-form",
   "kind": "external",
   "name": "TanStack Form",
   "import": "import { useForm } from \"@tanstack/react-form\"",
   "summary": "TanStack Form (import { useForm } from \"@tanstack/react-form\")",
   "fields": {
    "names": "tanstack form tan stack form ",
    "useWhen": "",
    "routed": "form state, validation and typed error objects",
    "do": ""
   },
   "notFor": [],
   "related": [],
   "exports": [],
   "aliases": [
    "react-form"
   ],
   "tokens": 30
  },
  {
   "id": "lucide",
   "kind": "external",
   "name": "Lucide",
   "import": "import { SearchIcon } from \"lucide-react\"",
   "summary": "Generic glyphs; Tecton domain glyphs come from @tecton/react/icons.",
   "fields": {
    "names": "lucide lucide ",
    "useWhen": "Generic glyphs; Tecton domain glyphs come from @tecton/react/icons.",
    "routed": "a glyph for an action rather than a key",
    "do": ""
   },
   "notFor": [],
   "related": [],
   "exports": [],
   "aliases": [
    "lucide-react"
   ],
   "tokens": 41
  },
  {
   "id": "tecton-icons",
   "kind": "external",
   "name": "Tecton icons",
   "import": "import { WellIcon } from \"@tecton/react/icons\"",
   "summary": "Tecton icons (import { WellIcon } from \"@tecton/react/icons\")",
   "fields": {
    "names": "tecton icons tecton icons christmas tree icon drill bit icon fault icon geobodies icon geostructure icon horizon icon log curve icon oil rig offshore icon rock formations icon seismic icon strata icon surface icon trajectory icon valve icon velocity model icon well icon well pick icon well plan icon",
    "useWhen": " wellhead 'christmas tree' valve stack: vertical pipe with stacked crossbars drill bit: tapered bit head with shank diagonal line with offset crossing segment (geologic fault) cluster of three hexagons irregular hexagon-like blob outline (geologic structure) single wave line (seismic horizon) vertical zig-zag curve (well log trace) offshore platform: derrick on deck over water triangle with layered strata lines (rock formations) horizontal line over concentric arcs (seismic reflection) stacked layers with colored (pink-orange gradient) top layer curved 3D mesh/grid surface curve from vertical to horizontal (well trajectory) valve symbol: pipe with T-handle stem upward arrow through layered arcs (velocity model) wellhead symbol: vertical line with two short side bars vertical line crossed by a horizontal pick marker with dot dotted vertical line with wellhead symbol (planned well)",
    "routed": "a glyph that stands for a thing rather than a person",
    "do": ""
   },
   "notFor": [],
   "related": [],
   "exports": [
    "ChristmasTreeIcon",
    "DrillBitIcon",
    "FaultIcon",
    "GeobodiesIcon",
    "GeostructureIcon",
    "HorizonIcon",
    "LogCurveIcon",
    "OilRigOffshoreIcon",
    "RockFormationsIcon",
    "SeismicIcon",
    "StrataIcon",
    "SurfaceIcon",
    "TrajectoryIcon",
    "ValveIcon",
    "VelocityModelIcon",
    "WellIcon",
    "WellPickIcon",
    "WellPlanIcon"
   ],
   "aliases": [
    "icons"
   ],
   "tokens": 371
  },
  {
   "id": "conventions",
   "kind": "topic",
   "name": "Prop conventions: handlers, state, Root/Trigger/Content, render, state attributes",
   "summary": "Prop conventions: handlers, state, Root/Trigger/Content, render, state attributes",
   "fields": {
    "names": "conventions",
    "useWhen": "The prop conventions every @tecton/react component follows, for agents whose prior is Radix shadcn or an older React Aria Tecton. Covers onClick (not onPress), disabled (not isDisabled), checked / onCheckedChange (not isSelected / onChange), pressed / onPressedChange, the controlled selected state, value / defaultValue / onValueChange with arrays for multi-value and value as an item's identity (not id, selectedKey or onSelectionChange), open / defaultOpen / onOpenChange on the root, the Root + Trigger + Content structure of Dialog, AlertDialog, Sheet, Drawer, Popover, HoverCard, Tooltip, DropdownMenu and ContextMenu (no DialogTrigger wrapper, no asChild), render={<Button />} for element polymorphism, side / align / sideOffset placement, the presence state attributes to style on (data-open, data-checked, data-active, data-pressed, data-highlighted, data-disabled), TectonProvider for direction, locale, router and portals, and the absence of any keyboard shortcut API. Load before writing any prop, handler, controlled state or state selector on a Tecton component.",
    "routed": "",
    "do": "The map Root, Trigger, Content render State attributes Providers, portals, shortcuts Common Mistakes [CRITICAL] React Aria handler and state names [HIGH] A trigger wrapping the overlay [HIGH] Items identified by id [HIGH] Importing the library underneath"
   },
   "notFor": [],
   "related": [],
   "exports": [],
   "aliases": [],
   "tokens": 1673
  },
  {
   "id": "rules",
   "kind": "topic",
   "name": "Rules for every file: imports, setup, palette, className, variants, icons",
   "summary": "Rules for every file: imports, setup, palette, className, variants, icons",
   "fields": {
    "names": "rules",
    "useWhen": "Foundation for writing any UI with @tecton/react. Covers the import paths (@tecton/react/components/<name>, @tecton/react/tecton/<name>, @tecton/react/icons — there is no root export, and nothing is imported from the libraries underneath), the @tecton/react/globals.css stylesheet and the dark-first `dark` class, why components are never installed with `shadcn add`, the reset Tailwind palette where bg-red-500 and text-zinc-400 emit no CSS at all, the semantic tokens (bg-primary, text-muted-foreground, text-destructive, bg-warning-surface) and the Tecton palette steps (bg-blue-120, text-blue-830), the rule that variants own colour, shape, size and padding while className carries layout only and no stylesheet targets a generated component's data-slot, the Tecton variant axes on Alert, Badge, Separator, Input, Textarea and SelectTrigger, data-icon=\"inline-start\" spacing, and Tecton domain glyphs versus Lucide. Load before writing or editing any Tecton markup, className, import or stylesheet.",
    "routed": "",
    "do": "Before you finish Setup Three import namespaces, no root export Colour comes from tokens, never from Tailwind's stock palette Variants own the look, className carries layout Icons Common Mistakes [CRITICAL] Stock Tailwind colour renders nothing [CRITICAL] Installing a component with the shadcn CLI [HIGH] Importing from the package root [HIGH] className overriding what a variant owns [HIGH] Hand-built status colours where a variant exists Looking further"
   },
   "notFor": [],
   "related": [],
   "exports": [],
   "aliases": [],
   "tokens": 2937
  },
  {
   "id": "theming",
   "kind": "topic",
   "name": "Colour, tokens, dark and light mode, ThemeRoot, micro-frontends",
   "summary": "Colour, tokens, dark and light mode, ThemeRoot, micro-frontends",
   "fields": {
    "names": "theming",
    "useWhen": "How colour reaches the screen in @tecton/react and what an application may and may not change. Covers the shadcn CSS variables (--background, --primary, --ring, --success-surface, --border-strong) as the only carrier of the theme and their generation from tokens/tecton.map.json, why an application never declares a --color-* and a missing colour is a token request, the fifteen contrast ramps and how to read a step (bg-blue-120 text-blue-830, steps 50 to 1570, no dark: pair), dark-first with the dark class on html, inverting a section with the light or dark class, ThemeRoot with theme=\"inherit\" | \"dark\" | \"light\" for an independently deployed application, and micro-frontends with @tecton/react/styles/scoped.css, the @tecton/react/postcss/scope plugin and @tecton/react/federation/shared. Load when picking a colour, adding a token, toggling or inverting the mode, retinting a subtree, or mounting one Tecton application inside another.",
    "routed": "",
    "do": "Setup Reading the palette Inverting a section ThemeRoot: an independently deployed application Micro-frontends: scoped utilities Common Mistakes [CRITICAL] Declaring a --color-* in the application stylesheet [HIGH] A dark: pair on a palette step [HIGH] Overriding a shadcn variable with an inline style [HIGH] globals.css inside an embedded remote [MEDIUM] ThemeRoot as the root of a standalone application"
   },
   "notFor": [],
   "related": [],
   "exports": [],
   "aliases": [],
   "tokens": 2851
  }
 ]
}
