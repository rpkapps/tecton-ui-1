// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Expand` (manifest: lucide: "expand").
// Drop icons-src/outlined/expand-content.svg in place and re-run `pnpm icons:build`.
import { Expand as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** ExpandContentIcon — two corner brackets with short arrows pointing outward */
export function ExpandContentIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="expand-content"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
ExpandContentIcon.displayName = "ExpandContentIcon"
