// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `FolderPlus` (manifest: lucide: "folder-plus").
// Drop icons-src/outlined/folder-new.svg in place and re-run `pnpm icons:build`.
import { FolderPlus as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** FolderNewIcon — folder with plus */
export function FolderNewIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="folder-new"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
FolderNewIcon.displayName = "FolderNewIcon"
