// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Columns3` (manifest: lucide: "columns-3").
// Drop icons-src/outlined/view-column.svg in place and re-run `pnpm icons:build`.
import { Columns3 as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** ViewColumnIcon — rectangle split into three vertical columns */
export function ViewColumnIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="view-column"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
ViewColumnIcon.displayName = "ViewColumnIcon"
