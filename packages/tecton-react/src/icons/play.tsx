// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Play` (manifest: lucide: "play").
// Drop icons-src/outlined/play.svg in place and re-run `pnpm icons:build`.
import { Play as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** PlayIcon — right-pointing triangle */
export function PlayIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="play"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
PlayIcon.displayName = "PlayIcon"
