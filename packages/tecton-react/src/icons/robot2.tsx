// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Bot` (manifest: lucide: "bot").
// Drop icons-src/outlined/robot2.svg in place and re-run `pnpm icons:build`.
import { Bot as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** Robot2Icon — robot head with eyes and separate body bar */
export function Robot2Icon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="robot2"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
Robot2Icon.displayName = "Robot2Icon"
