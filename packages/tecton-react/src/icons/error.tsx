// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `CircleAlert` (manifest: lucide: "circle-alert").
// Drop icons-src/outlined/error.svg in place and re-run `pnpm icons:build`.
import { CircleAlert as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** ErrorIcon — circle with exclamation mark */
export function ErrorIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="error"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
ErrorIcon.displayName = "ErrorIcon"
