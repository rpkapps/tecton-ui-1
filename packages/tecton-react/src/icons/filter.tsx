// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Funnel` (manifest: lucide: "funnel").
// Drop icons-src/outlined/filter.svg in place and re-run `pnpm icons:build`.
import { Funnel as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** FilterIcon — funnel */
export function FilterIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="filter"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
FilterIcon.displayName = "FilterIcon"
