// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Menu` (manifest: lucide: "menu").
// Drop icons-src/outlined/menu.svg in place and re-run `pnpm icons:build`.
import { Menu as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** MenuIcon — three horizontal lines */
export function MenuIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="menu"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
MenuIcon.displayName = "MenuIcon"
