// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Code` (manifest: lucide: "code").
// Drop icons-src/outlined/code.svg in place and re-run `pnpm icons:build`.
import { Code as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** CodeIcon — angle brackets with slash */
export function CodeIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="code"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
CodeIcon.displayName = "CodeIcon"
