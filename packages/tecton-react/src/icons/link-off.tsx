// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Unlink` (manifest: lucide: "unlink").
// Drop icons-src/outlined/link-off.svg in place and re-run `pnpm icons:build`.
import { Unlink as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** LinkOffIcon — chain link with diagonal strike */
export function LinkOffIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="link-off"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
LinkOffIcon.displayName = "LinkOffIcon"
