// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Minus` (manifest: lucide: "minus").
// Drop icons-src/outlined/remove.svg in place and re-run `pnpm icons:build`.
import { Minus as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** RemoveIcon — minus sign */
export function RemoveIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="remove"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
RemoveIcon.displayName = "RemoveIcon"
