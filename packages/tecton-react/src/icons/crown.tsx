// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Crown` (manifest: lucide: "crown").
// Drop icons-src/outlined/crown.svg in place and re-run `pnpm icons:build`.
import { Crown as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** CrownIcon — crown outline */
export function CrownIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="crown"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
CrownIcon.displayName = "CrownIcon"
