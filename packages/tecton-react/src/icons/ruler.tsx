// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Ruler` (manifest: lucide: "ruler").
// Drop icons-src/outlined/ruler.svg in place and re-run `pnpm icons:build`.
import { Ruler as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** RulerIcon — right-triangle set square with tick marks */
export function RulerIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="ruler"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
RulerIcon.displayName = "RulerIcon"
