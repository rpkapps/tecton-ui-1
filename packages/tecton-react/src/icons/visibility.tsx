// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Eye` (manifest: lucide: "eye").
// Drop icons-src/outlined/visibility.svg in place and re-run `pnpm icons:build`.
import { Eye as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** VisibilityIcon — eye */
export function VisibilityIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="visibility"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
VisibilityIcon.displayName = "VisibilityIcon"
