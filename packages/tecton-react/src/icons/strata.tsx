// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Layers` (manifest: lucide: "layers").
// Drop icons-src/outlined/strata.svg in place and re-run `pnpm icons:build`.
import { Layers as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** StrataIcon — stacked layers with colored (pink-orange gradient) top layer */
export function StrataIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="strata"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
StrataIcon.displayName = "StrataIcon"
