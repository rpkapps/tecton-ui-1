// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `MousePointer` (manifest: lucide: "mouse-pointer").
// Drop icons-src/outlined/select-cursor.svg in place and re-run `pnpm icons:build`.
import { MousePointer as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** SelectCursorIcon — mouse pointer arrow */
export function SelectCursorIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="select-cursor"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
SelectCursorIcon.displayName = "SelectCursorIcon"
