// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Plus` (manifest: lucide: "plus").
// Drop icons-src/outlined/add.svg in place and re-run `pnpm icons:build`.
import { Plus as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** AddIcon — plus sign */
export function AddIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="add"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
AddIcon.displayName = "AddIcon"
