// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `History` (manifest: lucide: "history").
// Drop icons-src/outlined/history.svg in place and re-run `pnpm icons:build`.
import { History as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** HistoryIcon — clock with counter-clockwise arrow */
export function HistoryIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="history"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
HistoryIcon.displayName = "HistoryIcon"
