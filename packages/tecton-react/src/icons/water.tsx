// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Droplet` (manifest: lucide: "droplet").
// Drop icons-src/outlined/water.svg in place and re-run `pnpm icons:build`.
import { Droplet as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** WaterIcon — water droplet */
export function WaterIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="water"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
WaterIcon.displayName = "WaterIcon"
