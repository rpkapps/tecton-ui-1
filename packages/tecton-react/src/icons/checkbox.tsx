// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `SquareCheck` (manifest: lucide: "square-check").
// Drop icons-src/outlined/checkbox.svg in place and re-run `pnpm icons:build`.
import { SquareCheck as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** CheckboxIcon — square with check mark */
export function CheckboxIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="checkbox"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
CheckboxIcon.displayName = "CheckboxIcon"
