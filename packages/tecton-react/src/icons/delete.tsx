// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Trash2` (manifest: lucide: "trash-2").
// Drop icons-src/outlined/delete.svg in place and re-run `pnpm icons:build`.
import { Trash2 as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** DeleteIcon — trash can */
export function DeleteIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="delete"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
DeleteIcon.displayName = "DeleteIcon"
