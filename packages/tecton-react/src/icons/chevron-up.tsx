// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `ChevronUp` (manifest: lucide: "chevron-up").
// Drop icons-src/outlined/chevron-up.svg in place and re-run `pnpm icons:build`.
import { ChevronUp as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** ChevronUpIcon — chevron pointing up */
export function ChevronUpIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="chevron-up"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
ChevronUpIcon.displayName = "ChevronUpIcon"
