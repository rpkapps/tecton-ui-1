// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `PencilRuler` (manifest: lucide: "pencil-ruler").
// Drop icons-src/outlined/design.svg in place and re-run `pnpm icons:build`.
import { PencilRuler as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** DesignIcon — crossed pencil and ruler */
export function DesignIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="design"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
DesignIcon.displayName = "DesignIcon"
