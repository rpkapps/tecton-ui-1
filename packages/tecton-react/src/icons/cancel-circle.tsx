// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `CircleX` (manifest: lucide: "circle-x").
// Drop icons-src/outlined/cancel-circle.svg in place and re-run `pnpm icons:build`.
import { CircleX as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** CancelCircleIcon — circle with x */
export function CancelCircleIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="cancel-circle"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
CancelCircleIcon.displayName = "CancelCircleIcon"
