// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Grip` (manifest: lucide: "grip").
// Drop icons-src/outlined/apps.svg in place and re-run `pnpm icons:build`.
import { Grip as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** AppsIcon — 3x3 grid of dots */
export function AppsIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="apps"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
AppsIcon.displayName = "AppsIcon"
