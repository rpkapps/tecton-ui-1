// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `SquareDashed` (manifest: lucide: "square-dashed").
// Drop icons-src/outlined/select.svg in place and re-run `pnpm icons:build`.
import { SquareDashed as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** SelectIcon — dashed square outline */
export function SelectIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="select"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
SelectIcon.displayName = "SelectIcon"
