// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Square` (manifest: lucide: "square").
// Drop icons-src/outlined/checkbox-outline-blank.svg in place and re-run `pnpm icons:build`.
import { Square as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** CheckboxOutlineBlankIcon — empty square outline */
export function CheckboxOutlineBlankIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="checkbox-outline-blank"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
CheckboxOutlineBlankIcon.displayName = "CheckboxOutlineBlankIcon"
