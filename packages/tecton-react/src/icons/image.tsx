// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Image` (manifest: lucide: "image").
// Drop icons-src/outlined/image.svg in place and re-run `pnpm icons:build`.
import { Image as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** ImageIcon — picture frame with mountains */
export function ImageIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="image"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
ImageIcon.displayName = "ImageIcon"
