// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Columns2` (manifest: lucide: "columns-2").
// Drop icons-src/outlined/splitscreen.svg in place and re-run `pnpm icons:build`.
import { Columns2 as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** SplitscreenIcon — two side-by-side panels with small plus */
export function SplitscreenIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="splitscreen"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
SplitscreenIcon.displayName = "SplitscreenIcon"
