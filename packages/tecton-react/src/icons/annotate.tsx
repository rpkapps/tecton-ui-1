// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Highlighter` (manifest: lucide: "highlighter").
// Drop icons-src/outlined/annotate.svg in place and re-run `pnpm icons:build`.
import { Highlighter as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** AnnotateIcon — diagonal marker pen */
export function AnnotateIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="annotate"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
AnnotateIcon.displayName = "AnnotateIcon"
