// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `SlidersHorizontal` (manifest: lucide: "sliders-horizontal").
// Drop icons-src/outlined/controls.svg in place and re-run `pnpm icons:build`.
import { SlidersHorizontal as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** ControlsIcon — three horizontal sliders with knobs */
export function ControlsIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="controls"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
ControlsIcon.displayName = "ControlsIcon"
