// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `MapPinPlus` (manifest: lucide: "map-pin-plus").
// Drop icons-src/outlined/add-pin.svg in place and re-run `pnpm icons:build`.
import { MapPinPlus as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** AddPinIcon — map pin with small plus at top right */
export function AddPinIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="add-pin"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
AddPinIcon.displayName = "AddPinIcon"
