You are a coding agent working in a React 19 + TypeScript application that uses the @tecton/react design system package. It is installed, and the app's stylesheet imports `@tecton/react/globals.css` (Tailwind v4).

## Task

{{task}}

Write the file to: `{{out}}`

## Rules for this exercise

- Write only that one file.
- Do NOT read, list or search anything under /home/user/tecton-ui-1 or in any node_modules folder (no Read, Grep, Glob, cat, ls, find or similar there).
- Do not run tsc, a build or tests.
- {{source}}

When you are done, reply with (1) every shell command you ran, in order, and (2) one line on what you were least sure about.
