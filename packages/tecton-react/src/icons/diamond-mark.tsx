// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Diamond` (manifest: lucide: "diamond").
// Drop icons-src/outlined/diamond-mark.svg in place and re-run `pnpm icons:build`.
import { Diamond as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** DiamondMarkIcon — diamond (rotated square) outline */
export function DiamondMarkIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="diamond-mark"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
DiamondMarkIcon.displayName = "DiamondMarkIcon"
