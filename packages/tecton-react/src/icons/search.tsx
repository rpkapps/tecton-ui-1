// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Search` (manifest: lucide: "search").
// Drop icons-src/outlined/search.svg in place and re-run `pnpm icons:build`.
import { Search as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** SearchIcon — magnifying glass */
export function SearchIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="search"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
SearchIcon.displayName = "SearchIcon"
