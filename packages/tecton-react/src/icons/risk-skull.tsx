// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Skull` (manifest: lucide: "skull").
// Drop icons-src/outlined/risk-skull.svg in place and re-run `pnpm icons:build`.
import { Skull as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** RiskSkullIcon — skull with three list lines to the right */
export function RiskSkullIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="risk-skull"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
RiskSkullIcon.displayName = "RiskSkullIcon"
