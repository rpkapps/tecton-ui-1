// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `X` (manifest: lucide: "x").
// Drop icons-src/outlined/close.svg in place and re-run `pnpm icons:build`.
import { X as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** CloseIcon — x cross */
export function CloseIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="close"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
CloseIcon.displayName = "CloseIcon"
