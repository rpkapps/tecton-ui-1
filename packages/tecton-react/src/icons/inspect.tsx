// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `ScanSearch` (manifest: lucide: "scan-search").
// Drop icons-src/outlined/inspect.svg in place and re-run `pnpm icons:build`.
import { ScanSearch as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** InspectIcon — scan corners with magnifier in center */
export function InspectIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="inspect"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
InspectIcon.displayName = "InspectIcon"
