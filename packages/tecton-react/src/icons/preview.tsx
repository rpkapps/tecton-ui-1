// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `ScanEye` (manifest: lucide: "scan-eye").
// Drop icons-src/outlined/preview.svg in place and re-run `pnpm icons:build`.
import { ScanEye as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** PreviewIcon — eye inside a rounded window */
export function PreviewIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="preview"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
PreviewIcon.displayName = "PreviewIcon"
