// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Settings` (manifest: lucide: "settings").
// Drop icons-src/outlined/settings.svg in place and re-run `pnpm icons:build`.
import { Settings as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** SettingsIcon — gear */
export function SettingsIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="settings"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
SettingsIcon.displayName = "SettingsIcon"
