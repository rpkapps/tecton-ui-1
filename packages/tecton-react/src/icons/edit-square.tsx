// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `SquarePen` (manifest: lucide: "square-pen").
// Drop icons-src/outlined/edit-square.svg in place and re-run `pnpm icons:build`.
import { SquarePen as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** EditSquareIcon — square with pencil over top right corner */
export function EditSquareIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="edit-square"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
EditSquareIcon.displayName = "EditSquareIcon"
