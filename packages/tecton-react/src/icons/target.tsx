// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `Focus` (manifest: lucide: "focus").
// Drop icons-src/outlined/target.svg in place and re-run `pnpm icons:build`.
import { Focus as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** TargetIcon — scan corners with circle in center */
export function TargetIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="target"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
TargetIcon.displayName = "TargetIcon"
