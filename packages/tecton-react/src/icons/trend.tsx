// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `ChartNoAxesCombined` (manifest: lucide: "chart-no-axes-combined").
// Drop icons-src/outlined/trend.svg in place and re-run `pnpm icons:build`.
import { ChartNoAxesCombined as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** TrendIcon — rising line chart over bar columns */
export function TrendIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="trend"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
TrendIcon.displayName = "TrendIcon"
