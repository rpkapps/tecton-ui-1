// GENERATED by scripts/build-icons.mts — do not edit
// No SVG source yet — renders lucide-react's `CirclePlus` (manifest: lucide: "circle-plus").
// Drop icons-src/outlined/add-circle.svg in place and re-run `pnpm icons:build`.
import { CirclePlus as Fallback } from "lucide-react"
import type { TectonIconProps } from "./types"

/** AddCircleIcon — circle with plus */
export function AddCircleIcon({
  size = 24,
  strokeWidth,
  absoluteStrokeWidth,
  variant = "outlined",
  className,
  ...props
}: TectonIconProps) {
  return (
    <Fallback
      size={size}
      strokeWidth={strokeWidth}
      absoluteStrokeWidth={absoluteStrokeWidth}
      className={className}
      data-tecton-icon="add-circle"
      data-tecton-source="lucide-fallback"
      data-tecton-variant={variant}
      {...props}
    />
  )
}
AddCircleIcon.displayName = "AddCircleIcon"
